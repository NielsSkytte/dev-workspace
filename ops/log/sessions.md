# Session Log

Chronological record of workspace sessions — what was done, decided, and what's next. Written at session end (via `/log`, owned by M); the session-start walk reads the latest entry for continuity. **Newest entries at the bottom.**

---

## 2026-06-11
- **Did:** Stood up the ICOR capture→task pipeline. Built `/park` → `ops/TODO.md` (Input) and `/task` → `ops/tasks/` store (Output; open/in-progress/done/cancelled). Created `AGENTS.md` (LLM-agnostic principle) and relocated substrate out of `.claude/` into `ops/`. Renamed the main inbox to `ops/TODO.md` (per-project `INBOX.md` stays the curated knowledge feed via `/brief`). Built the continuity loop: this session log, the session-start walk (SessionStart hook + M routine), and `/log`. Deleted a stray mangled gist file in AtomicCortex.
- **Decided:** ADR-0001 (`own/AtomicCortex`) — AtomicCortex is a *system*, not a project; peer to the personal PKA; "one spec, many instances, federated at seams." LLM-agnostic-substrate / harness-as-accelerator is now Guardrail 7; continuity loop is Guardrail 8.
- **Tasks:** open `2026-06-10-migrate-dev-skills-to-atomiccortex`; in-progress `2026-06-11-complete-llm-agnostic-split` (part 1 substrate relocation done; part 2 = migrate `CLAUDE.md` conventions → `AGENTS.md`).
- **Next:** test the continuity loop in a fresh session; then finish part 2 of the LLM-agnostic split. Optional: extract task-store machinery into the shared spec for AtomicCortex.

## 2026-06-15
- **Did:** Exercised the full capture->triage loop end-to-end. Captured 4 todos; triaged 2 into tasks. Revised `/todo` to a two-beat capture (verbatim-first, then one optional `AskUserQuestion` "sharpen" beat only when a line is too thin to triage — best-guess options + free-text "Other"). Fixed a substrate naming gap: `_TEMPLATE.md` `source:` enum now `todo|inbox|direct` and `/task` step 3 routes a TODO.md capture to `source: todo` with a backlink (previously mis-pointed at `INBOX.md`). Scaffolded placeholder project `customers/Melbye/data-agent-offer` (CLAUDE.md + CONTEXT.md + VS Code task).
- **Decided:** New convention in `AGENTS.md` — *"A referenced customer always has a project"*: a customer named with no folder gets a placeholder project scaffolded immediately (not a full interview, not workspace-level orphan). Wired into `/task` triage. Deliberately NOT mirrored to Claude memory (substrate is the single home; avoids drift — Guardrail 7). Confirmed capture-thinness and triage-readiness are different bars (a line can pass capture yet arrive under-specified at triage — esp. client work, where the fix is a project home, not a harder capture gate).
- **Tasks:** created `2026-06-15-task-assisted-triage` (open, owner Q) and `2026-06-15-melbye-data-agent-offer` (open, owner content, project customers/Melbye/data-agent-offer). Still open in TODO.md: session time-tracking capture (deferred to later triage).
- **Next:** restart a fresh session to verify the continuity loop + revised `/todo` + customer-project rule all fire correctly. Then: build the assisted-triage enhancement (decide trigger: threshold vs daily schedule; routine must land in AGENTS.md). Triage the session-time-tracking todo. Flesh out Melbye via full `/new-project` interview once the deal is real.

### Session 2 (continuity-loop verification)
- **Did:** Verified the continuity loop in a fresh restart — `SessionStart` hook fired correctly and the workspace walk surfaced open work. Clarified two harness behaviours: (1) the hook primes context but does not auto-generate a reply, so the walk appears on the first user message; (2) image paste fails in the VS Code *integrated terminal* (text-only paste) — use the VS Code extension, drag-drop, or a file path. **Finished Part 2 of the LLM-agnostic split:** migrated all operational content (owner, folder structure, buckets, project files/types/scale/identity, agent roster, capability rubric, fabric note, knowledge flow, guardrails 1-9, start/switch process) into `AGENTS.md` as the full operational doc, and rewrote root `CLAUDE.md` as a thin Claude bootstrap (harness map + Claude-only continuity triggers, keeping the cascading project-walk rule). Acid test holds.
- **Decided:** Response-style preference captured to memory — `terse`/`brief`/`full` sticky steering keywords (default `brief`); no edit narration unless asked.
- **Tasks:** `2026-06-11-complete-llm-agnostic-split` → **done** (moved to `ops/tasks/done/`). In-progress queue now empty.
- **Next:** build assisted-triage (`2026-06-15-task-assisted-triage`, owner Q) — decide trigger (threshold vs daily schedule); routine lands in AGENTS.md. Triage the 2 unprocessed TODOs (better email creation; session time-tracking). Flesh out Melbye once the deal is real.

## 2026-06-18
- **Did:** Tystofte project — major Data-Discovery build + rescope. Resumed a stale CONTEXT (last 06-02) against git reality (domains + objekt_kategori already pushed to Fabric `develop`). Discovered and consolidated the Excel handover pipeline into one builder: `build_workbook.py` → the single sheet-protected review workbook `Tystofte_review.xlsx` (sheets Tabeller + Visninger, agreed manual columns), refreshed by a deterministic Python rebuild + keyed merge — verified manual data and row order survive a refresh. Added `explain_views.py` (Claude API, `claude-opus-4-8`) to close the views-refresh gap + a coverage check. **Rescoped Tystofte to an umbrella engagement** (Data-Discovery / Tystofte-Fabric / PowerPortal.wiki; data migration is future/out-of-scope) and split **Data-Discovery** into its own local git repo (moved `_build`, the xlsx, and `obsolete/` out of the wiki; repointed all paths; committed). Moved 11 superseded files to `obsolete/`.
- **Decided:** Review-workbook refresh = Python rebuild + keyed merge, **not** Power Query (openpyxl strips the Fabric DataMashup; views were never a PQ source). Data-Discovery is its own repo. Wiki is read-mostly **input** with **scoped updates only** (never a full sync) since it now holds third-party content. (Full decision log in `customers/Tystofte/CONTEXT.md`.)
- **Tasks:** none in the workspace store — Tystofte is tracked via its own `CONTEXT.md`/`INBOX.md`. Created `customers/Tystofte/INBOX.md` with the wiki scoped-update item (= Tystofte Next Action #1).
- **Next:** define which `PowerPortal.wiki/Handover/` paths Pingala owns + build a safe selective commit/push (never `git add -A` on the wiki); deliver `Tystofte_review.xlsx` to Tystofte for fill-in.

## 2026-06-19
- **Did:** Built an interactive **skill-tree** visualization (`ops/skill-tree/`): a self-contained, fully-offline HTML graph (vendored `vis-network.min.js`) of all 37 skills colored by category (Microsoft / own-Fabric / own-non-Fabric), with existing references (solid) vs proposed/overlap references (dashed), click-to-trace, and full descriptions on hover/click (`skill-data.js`). Reviewed all 10 `bundle: custom` skills for missing cross-references **and** reviewed Microsoft-vs-own for overlap. Applied **8 high-confidence links** as additive "Related skills" sections across 5 custom skills (pipeline trio cross-refs: fabric-pipeline-notebook ↔ timestamp-timezone-pipelines ↔ medallion-migration-validation; playbook → pingala-fabric-platform back-link; pingala-fabric-platform → pingala-visual-identity; own→MS overlap links to spark-operations-cli and e2e-medallion-architecture). Fixed `pingala-fabric-platform`'s 8 **dangling `references/*.md` pointers** (git confirms the dir never existed) by reframing them as an "areas to explore later" roadmap. Diagnosed the `skill-creator` plugin.
- **Decided:** Skill classifier = the `bundle:` frontmatter field (`custom` = our own, `fabric-*`/`powerbi-*` = Microsoft). No true MS↔own duplication — MS skills are CLI *execution*, ours are operational *judgement/methodology*; the fix is to **link**, not merge/delete. Never run skill-creator/generators on the vendored MS skills (submodule — edits clobbered by `/update-skills`); custom skills only. Reference deep-dives stay **inline** (no `references/` split) — reframed as future exploration areas. `skill-creator` is installed + enabled but needs a **full CC restart** (not `/reload-plugins`) to register as an invocable skill.
- **Tasks:** none in the workspace store. TODO.md: added `2026-06-18 — hør Simon om hans claude skills`.
- **Next:** Optionally apply the medium/low-confidence links still shown as dashed in the skill tree (azure-devops-backlog / email-outlook-ready → pingala-visual-identity; medallion → synapse/databricks migration; pingala-fabric-platform → semantic-model / spark / sqldw authoring). Restart Claude Code to make `skill-creator` invocable, then optionally use its eval/variance-analysis feature to test description-triggering accuracy on the custom skills. Follow up on the Simon TODO.

### Session 2 (Fabric Data Agent skills)
- **Did:** Built **3 new custom skills** for building production-grade Microsoft Fabric Data Agents (internal delivery / PoC scope, not sales): `fabric-data-agent` (spine + 4 references: prep-for-ai-vs-agent, instructions-cookbook, ontology-grounding, git-and-config-as-code), `fabric-data-agent-testing`, `fabric-data-agent-ops`; registered in `BUNDLES.md`. Grounded in current MS Learn via parallel research agents, **fact-checked** every baked number against live Learn pages (all 13 held), added a latest-changes pass (GA status, Preview Runtime lifting 25×25, GPT-5.X, MCP server, Foundry/Copilot Studio, SPN runtime preview, Purview query-time enforcement, Assistants→Responses API deadline **26 Aug 2026**), then **enhanced with attributed practitioner war-stories** (3 review agents → Pawar, Webb, Tabular Editor, Ilic, Lachev, Zavarella, Tejwani + MS-confirmed community threads) incl. real CU costs (~5–10k CU/question, not the doc's 400), mapping drift, SDK `thread_url`→`message_url`, no deployment-pipeline rebind. Built them through the real `skill-creator` generator.
- **Decided:** Sales is out of scope — these are the internal delivery playbook (offer is a separate deliverable). `skill-creator`'s **description-optimization loop yields no valid triggering signal on this Windows setup** (nested `claude -p` + command-stub detection reads 0 for all on-topic queries → identical should-not-count scores) and **crashes on emoji in SKILL.md** (cp1252 / byte 0x8f) — so descriptions kept **hand-authored** (pushy, explicit trigger lists, sibling cross-refs); logged to memory as `skill-creator-trigger-eval-limitation`. `/run-skill-generator` ≠ a general generator (it authors app-run skills); the real one is `skill-creator` (needed full CC restart to register).
- **Tasks:** none in the workspace store. Relates to `2026-06-15-melbye-data-agent-offer` (the skills are the reusable backbone under it).

### Session 3 (Melbye data-agent offer draft)
- **Did:** Drafted the **Melbye Fabric Data Agent offer** as Pingala SoW content markdown at `customers/Melbye/data-agent-offer/offer/Melbye_DataAgent_SoW_content.md` (mirrors the Joe & The Juice SoW structure so `/fill-sow` maps cleanly). Grounded the activities/deliverables in the 3 `fabric-data-agent*` skills. Scope = **one** data agent on a **dedicated scoped subset model** (a subset of the production model, created under the work order — keeps AI-enablement off production), covering `factSalesTransactions` + 14 listed dims + `Calculations > Sales` measures only. 7 activities (question catalogue → build subset model → Prep-for-AI → build/configure agent → validated test+eval+regression baseline → capacity sizing from DEV/trial split → handover). Refined estimate to land the ceiling under 200h: **160–189h** (trimmed top-end padding + the Atomic-composed subset build; protected the Prep-for-AI 45–52 and Test/Eval 30–38 quality core).
- **Decided:** Format = **Pingala SoW** (signable, not a deck/email) via the existing `/fill-sow` pipeline. Offer language **English**. WP 2879 (currency) + WP 2911 (business-unit/vectors) named as **recommended companion work packages, separately scoped** — not folded in; `DimBusinessUnitSegments` out of scope unless WP 2911 lands. Subset model built via the **Pingala Atomic "network/compose" feature** — but that feature is a **skill gap** (not in `pingala-fabric-platform`); kept the mechanism out of the client text, stated only the *requirement* for a separate model. Estimate kept honestly realistic (T&M → actuals bill regardless), ceiling deliberately **sub-200** so it stays under the customer's approval radar — protected by the Change-Request clause + tight Not-in-Scope.
- **Tasks:** none moved. Work sits under `2026-06-15-melbye-data-agent-offer`. User logged the Atomic-feature skill gap as a TODO separately.
- **Next:** **Paused for the user's review of the draft** before finalising. On approval: run `/fill-sow` (needs Melbye full legal name + named Pingala/customer contacts) to render the signable Word doc. Later: document the Atomic compose/"network" feature in `pingala-fabric-platform` (the logged TODO).
- **Next:** live-validate triggering by asking a data-agent question in a fresh session (the optimization loop can't measure it here); decide whether to keep or remove the `_data-agent-eval/` workspace (eval sets reusable if the harness is ever fixed); optionally fold the playbook into the Melbye offer deliverable.

### Session 3 (skill quality review against skill-creator rubric)
- **Did:** Reviewed all **13 non-Microsoft (`bundle: custom`) skills** against the skill-creator writing rubric (read its SKILL.md from the plugin cache; static quality pass, not the eval-loop), one reviewer agent per skill in parallel. Excluded the 28 vendored MS submodule skills. Result: 4 STRONG, 9 SOLID, none NEEDS WORK. Applied **8 correctness/handoff edits across 8 skills** (all uncommitted, working tree): `fabric-project-access` "seven"->"eight" workstreams + Step 2 routes the access-request email through `email-outlook-ready`; `fabric-pipeline-notebook` `<maxResults>` placeholder -> real `MAX_RESULTS` var (was a copy-paste `SyntaxError`); `pingala-project-playbook` Phase 6.1 -> `pingala-visual-identity`; `azure-devops-backlog` Data Excellence epic reconciled; `fabric-data-agent`+`-ops` git/config-as-code ownership split fixed. Captured the Matas data-compare user-stories write-up to TODO.md.
- **Decided:** Data Excellence epic = keep the **Pingala 6-step maturity journey** (Automate->Align->Validate->Adopt->Integrate->Transform) as the Features (original IP, beats the generic 12-dimension MS adoption-roadmap lift); Features in both SKILL.md tree and `push_backlog.py` now mirror the HTML. Git/config-as-code **belongs to `fabric-data-agent`** (where the 10.5 KB reference file already lived and `-ops` already deferred to it) — both frontmatters reworded so serialization mechanics = build skill, capacity/cost/logging/CI-CD-promotion = `-ops`. `skill-creator` plugin is on disk but not invocable this session (no restart) — applied its rubric manually.
- **Tasks:** none in the workspace store. TODO.md: added `2026-06-19 - write up the user stories on matas project data-compare with more detail on the project design etc.`
- **Next:** biggest recurring rubric gap is **deterministic work left un-scripted** — strong `scripts/` candidates: `fabric-rename-entity` (git+JSON rename), `medallion-migration-validation` (QC SQL), `fabric-data-agent-testing` (eval gate), `timestamp-timezone-pipelines`/`fabric-pipeline-notebook` snippets. Also: bundle `pingala-visual-identity` fonts (described-only); tighten `pingala-fabric-platform` over-broad triggers + drop leftover "explore later" scaffolding. Consider committing this session's skill edits.

## 2026-06-22
- **Did:** Took the **Melbye Fabric Data Agent SoW** from first draft to review-ready (`customers/Melbye/data-agent-offer/offer/Melbye_DataAgent_SoW_content.md`, now **v0.7**), plus a one-page **executive brief** (`Melbye_DataAgent_ExecBrief.md`, hours-only, Phase 2 indicative). Major moves: added the **dedicated subset-model** requirement; cut PM overhead + shrank the front-end (workshop->short question-types session; subset build is a scale-down); **restructured into two phases** — Phase 1 *build & validate a proper agent (try it)* vs Phase 2 *production readiness (optional)*; added **publish-to-Teams** to Phase 1 (hedged on EU data-residency sign-off); expanded + de-risked **logging/monitoring** (logging = feasibility-spike + SDK-fronted Delta table, **Purview NOT assumed**, flagged we've never built it). **Verified all Copilot/Teams licensing against live MS Learn** (installed the `microsoft-docs` MCP). Final estimate **Phase 1 106-128h / Phase 2 72-102h** (T&M, hours only — no rate on file).
- **Decided:** Customer is **Swedish (not Danish)** -> EU data-residency is a real decision point (M365 Copilot responses may leave the EU region; fallback = in-region Fabric chat pane). Licensing model confirmed from docs: **M365 Copilot-licensed users = agent usage included/zero-rated** (billing-rates table "No charge" column + footnote 1: B2E, runs under the user's identity, fair-use limits); **unlicensed users = metered Copilot Credits** (free Copilot Chat seat, per-use cost to the org via PAYG/prepaid packs). Both Teams routes (M365 Copilot Agent Store / Copilot Studio) are still **preview**. **F4 is now the Pingala default capacity for data-agent work** (recorded in `fabric-data-agent-ops` via subagent). New standing rule: **bump the doc version on every major revision** (saved as memory `feedback-version-bump-revisions`; demoed across v0.1->v0.7).
- **Tasks:** none moved; work sits under `2026-06-15-melbye-data-agent-offer`.
- **Next:** **Awaiting Niels' review** of SoW v0.7 + exec brief. On approval: `/fill-sow` to render the signable Word doc (needs Melbye full legal name + named Pingala/customer contacts). Open optional: document the Atomic compose/"network" feature in `pingala-fabric-platform` (logged TODO); decide whether to fold the B2E/identity/fair-use licensing nuance into the SoW (currently background only).

### Session 2 (memory system build)
- **Did:** Built and committed (`3f4ccd4`) the unified **Storage/Injection/Recall + skill-evaluation** memory system (Simon-Scrapes model) into one agnostic home at `ops/memory/`. Migrated the 12 Claude-native memories (`~/.claude/.../memory/`) into `ops/memory/store/` with a DB-ready record shape (`id/ts/type/scope/source/tags/status`); old location reduced to a stub. New: `ops/memory/README.md` (tool-neutral spec + by-hand recipes) and `daily/` (raw per-turn stream). **Capture** = `.claude/hooks/capture_turn.py` (Stop hook; per-turn; Haiku summary with a deterministic no-API fallback) — verified against the real transcript. **Injection** = `build_snapshot.py` emits a capped ASCII snapshot via the rewired `session-start.ps1`. **Recall** = keyword grep + cited source. Disabled native auto-memory. Updated `AGENTS.md` (Memory section + Guardrails 7/8), `CLAUDE.md`, `/log`, `/exit`. Designed via a Plan agent after the Ultraplan cloud handoff failed; executed from the approved local plan.
- **Decided:** One agnostic home in `ops/memory/` (markdown = source of truth; any index/DB rebuildable from it). **Recall = keyword now, DB-ready** (semantic deferred until a logged recall-miss). **Capture = per-turn auto-hook** (user's call: all DEV turns are work, so per-turn is signal not noise). **Migrate + hard-disable native auto-memory** via `autoMemoryEnabled: false` — confirmed disablable (corrected my earlier wrong "can't turn it off"). Implementation refinements: capture is one Python script (dodges the PS-5.1 ASCII footgun); snapshot output ASCII-normalized; skill-eval wired into AGENTS.md capability triggers (not `/update-skills`). No duplicate functionality — TODO/tasks/sessions.md/CONTEXT/INBOX untouched; `/log` now also distills `daily/`->`store/`.
- **Tasks:** none in the workspace store.
- **Next:** **Restart Claude Code** to activate the Stop hook + snapshot injection + the auto-memory-disable (hooks load at session start). Then verify: a record auto-lands in `daily/` after the first turn; `/memory` shows disabled; the snapshot appears in the preamble. If the Stop hook doesn't fire, switch the hook's `python` to an absolute interpreter path. Optional later: automate distillation; add SQLite-FTS / vector recall when keyword proves insufficient.
