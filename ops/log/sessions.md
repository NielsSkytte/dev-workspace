# Session Log

Chronological record of workspace sessions — what was done, decided, and what's next. Written at session end (via `/log`, owned by M); the session-start walk reads the latest entry for continuity. **Newest entries at the bottom.**

---

## 2026-06-11
- **Did:** Stood up the ICOR capture→task pipeline. Built `/park` → `ops/TODO.md` (Input) and `/task` → `ops/tasks/` store (Output; open/in-progress/done/cancelled). Created `AGENTS.md` (LLM-agnostic principle) and relocated substrate out of `.claude/` into `ops/`. Renamed the main inbox to `ops/TODO.md` (per-project `INBOX.md` stays the curated knowledge feed via `/brief`). Built the continuity loop: this session log, the session-start walk (SessionStart hook + M routine), and `/log`. Deleted a stray mangled gist file in AtomicCortex.
- **Decided:** ADR-0001 (`own/AtomicCortex`) — AtomicCortex is a *system*, not a project; peer to the personal PKA; "one spec, many instances, federated at seams." LLM-agnostic-substrate / harness-as-accelerator is now Guardrail 7; continuity loop is Guardrail 8.
- **Tasks:** open `2026-06-10-migrate-dev-skills-to-atomiccortex`; in-progress `2026-06-11-complete-llm-agnostic-split` (part 1 substrate relocation done; part 2 = migrate `CLAUDE.md` conventions → `AGENTS.md`).
- **Next:** test the continuity loop in a fresh session; then finish part 2 of the LLM-agnostic split. Optional: extract task-store machinery into the shared spec for AtomicCortex.

## 2026-06-15
- **Did:** Exercised the full capture->triage loop end-to-end. Captured 4 todos; triaged 2 into tasks. Revised `/todo` to a two-beat capture (verbatim-first, then one optional `AskUserQuestion` "sharpen" beat only when a line is too thin to triage — best-guess options + free-text "Other"). Fixed a substrate naming gap: `_TEMPLATE.md` `source:` enum now `todo|inbox|direct` and `/task` step 3 routes a TODO.md capture to `source: todo` with a backlink (previously mis-pointed at `INBOX.md`). Scaffolded placeholder project `customers/Melbye/data-agent-offer` (CLAUDE.md + CONTEXT.md + VS Code task).
- **Decided:** New convention in `AGENTS.md` — *"A referenced customer always has a project"*: a customer named with no folder gets a placeholder project scaffolded immediately (not a full interview, not workspace-level orphan). Wired into `/task` triage. Deliberately NOT mirrored to Claude memory (substrate is the single home; avoids drift — Guardrail 7). Confirmed capture-thinness and triage-readiness are different bars (a line can pass capture yet arrive under-specified at triage — esp. client work, where the fix is a project home, not a harder capture gate).
- **Tasks:** created `2026-06-15-task-assisted-triage` (open, owner Q) and `2026-06-15-melbye-data-agent-offer` (open, owner content, project customers/Melbye/data-agent-offer). Still open in TODO.md: session time-tracking capture (deferred to later triage).
- **Next:** restart a fresh session to verify the continuity loop + revised `/todo` + customer-project rule all fire correctly. Then: build the assisted-triage enhancement (decide trigger: threshold vs daily schedule; routine must land in AGENTS.md). Triage the session-time-tracking todo. Flesh out Melbye via full `/new-project` interview once the deal is real.

### Session 2 (continuity-loop verification)
- **Did:** Verified the continuity loop in a fresh restart — `SessionStart` hook fired correctly and the workspace walk surfaced open work. Clarified two harness behaviours: (1) the hook primes context but does not auto-generate a reply, so the walk appears on the first user message; (2) image paste fails in the VS Code *integrated terminal* (text-only paste) — use the VS Code extension, drag-drop, or a file path. **Finished Part 2 of the LLM-agnostic split:** migrated all operational content (owner, folder structure, buckets, project files/types/scale/identity, agent roster, capability rubric, fabric note, knowledge flow, guardrails 1-9, start/switch process) into `AGENTS.md` as the full operational doc, and rewrote root `CLAUDE.md` as a thin Claude bootstrap (harness map + Claude-only continuity triggers, keeping the cascading project-walk rule). Acid test holds.
- **Decided:** Response-style preference captured to memory — `terse`/`brief`/`full` sticky steering keywords (default `brief`); no edit narration unless asked.
- **Tasks:** `2026-06-11-complete-llm-agnostic-split` → **done** (moved to `ops/tasks/done/`). In-progress queue now empty.
- **Next:** build assisted-triage (`2026-06-15-task-assisted-triage`, owner Q) — decide trigger (threshold vs daily schedule); routine lands in AGENTS.md. Triage the 2 unprocessed TODOs (better email creation; session time-tracking). Flesh out Melbye once the deal is real.

## 2026-06-18
- **Did:** Tystofte project — major Data-Discovery build + rescope. Resumed a stale CONTEXT (last 06-02) against git reality (domains + objekt_kategori already pushed to Fabric `develop`). Discovered and consolidated the Excel handover pipeline into one builder: `build_workbook.py` → the single sheet-protected review workbook `Tystofte_review.xlsx` (sheets Tabeller + Visninger, agreed manual columns), refreshed by a deterministic Python rebuild + keyed merge — verified manual data and row order survive a refresh. Added `explain_views.py` (Claude API, `claude-opus-4-8`) to close the views-refresh gap + a coverage check. **Rescoped Tystofte to an umbrella engagement** (Data-Discovery / Tystofte-Fabric / PowerPortal.wiki; data migration is future/out-of-scope) and split **Data-Discovery** into its own local git repo (moved `_build`, the xlsx, and `obsolete/` out of the wiki; repointed all paths; committed). Moved 11 superseded files to `obsolete/`.
- **Decided:** Review-workbook refresh = Python rebuild + keyed merge, **not** Power Query (openpyxl strips the Fabric DataMashup; views were never a PQ source). Data-Discovery is its own repo. Wiki is read-mostly **input** with **scoped updates only** (never a full sync) since it now holds third-party content. (Full decision log in `customers/Tystofte/CONTEXT.md`.)
- **Tasks:** none in the workspace store — Tystofte is tracked via its own `CONTEXT.md`/`INBOX.md`. Created `customers/Tystofte/INBOX.md` with the wiki scoped-update item (= Tystofte Next Action #1).
- **Next:** define which `PowerPortal.wiki/Handover/` paths Pingala owns + build a safe selective commit/push (never `git add -A` on the wiki); deliver `Tystofte_review.xlsx` to Tystofte for fill-in.

## 2026-06-19
- **Did:** Built an interactive **skill-tree** visualization (`ops/skill-tree/`): a self-contained, fully-offline HTML graph (vendored `vis-network.min.js`) of all 37 skills colored by category (Microsoft / own-Fabric / own-non-Fabric), with existing references (solid) vs proposed/overlap references (dashed), click-to-trace, and full descriptions on hover/click (`skill-data.js`). Reviewed all 10 `bundle: custom` skills for missing cross-references **and** reviewed Microsoft-vs-own for overlap. Applied **8 high-confidence links** as additive "Related skills" sections across 5 custom skills (pipeline trio cross-refs: fabric-pipeline-notebook ↔ timestamp-timezone-pipelines ↔ medallion-migration-validation; playbook → pingala-fabric-platform back-link; pingala-fabric-platform → pingala-visual-identity; own→MS overlap links to spark-operations-cli and e2e-medallion-architecture). Fixed `pingala-fabric-platform`'s 8 **dangling `references/*.md` pointers** (git confirms the dir never existed) by reframing them as an "areas to explore later" roadmap. Diagnosed the `skill-creator` plugin.
- **Decided:** Skill classifier = the `bundle:` frontmatter field (`custom` = our own, `fabric-*`/`powerbi-*` = Microsoft). No true MS↔own duplication — MS skills are CLI *execution*, ours are operational *judgement/methodology*; the fix is to **link**, not merge/delete. Never run skill-creator/generators on the vendored MS skills (submodule — edits clobbered by `/update-skills`); custom skills only. Reference deep-dives stay **inline** (no `references/` split) — reframed as future exploration areas. `skill-creator` is installed + enabled but needs a **full CC restart** (not `/reload-plugins`) to register as an invocable skill.
- **Tasks:** none in the workspace store. TODO.md: added `2026-06-18 — hør Simon om hans claude skills`.
- **Next:** Optionally apply the medium/low-confidence links still shown as dashed in the skill tree (azure-devops-backlog / email-outlook-ready → pingala-visual-identity; medallion → synapse/databricks migration; pingala-fabric-platform → semantic-model / spark / sqldw authoring). Restart Claude Code to make `skill-creator` invocable, then optionally use its eval/variance-analysis feature to test description-triggering accuracy on the custom skills. Follow up on the Simon TODO.

### Session 2 (Fabric Data Agent skills)
- **Did:** Built **3 new custom skills** for building production-grade Microsoft Fabric Data Agents (internal delivery / PoC scope, not sales): `fabric-data-agent` (spine + 4 references: prep-for-ai-vs-agent, instructions-cookbook, ontology-grounding, git-and-config-as-code), `fabric-data-agent-testing`, `fabric-data-agent-ops`; registered in `BUNDLES.md`. Grounded in current MS Learn via parallel research agents, **fact-checked** every baked number against live Learn pages (all 13 held), added a latest-changes pass (GA status, Preview Runtime lifting 25×25, GPT-5.X, MCP server, Foundry/Copilot Studio, SPN runtime preview, Purview query-time enforcement, Assistants→Responses API deadline **26 Aug 2026**), then **enhanced with attributed practitioner war-stories** (3 review agents → Pawar, Webb, Tabular Editor, Ilic, Lachev, Zavarella, Tejwani + MS-confirmed community threads) incl. real CU costs (~5–10k CU/question, not the doc's 400), mapping drift, SDK `thread_url`→`message_url`, no deployment-pipeline rebind. Built them through the real `skill-creator` generator.
- **Decided:** Sales is out of scope — these are the internal delivery playbook (offer is a separate deliverable). `skill-creator`'s **description-optimization loop yields no valid triggering signal on this Windows setup** (nested `claude -p` + command-stub detection reads 0 for all on-topic queries → identical should-not-count scores) and **crashes on emoji in SKILL.md** (cp1252 / byte 0x8f) — so descriptions kept **hand-authored** (pushy, explicit trigger lists, sibling cross-refs); logged to memory as `skill-creator-trigger-eval-limitation`. `/run-skill-generator` ≠ a general generator (it authors app-run skills); the real one is `skill-creator` (needed full CC restart to register).
- **Tasks:** none in the workspace store. Relates to `2026-06-15-melbye-data-agent-offer` (the skills are the reusable backbone under it).

### Session 3 (Melbye data-agent offer draft)
- **Did:** Drafted the **Melbye Fabric Data Agent offer** as Pingala SoW content markdown at `customers/Melbye/data-agent-offer/offer/Melbye_DataAgent_SoW_content.md` (mirrors the Joe & The Juice SoW structure so `/fill-sow` maps cleanly). Grounded the activities/deliverables in the 3 `fabric-data-agent*` skills. Scope = **one** data agent on a **dedicated scoped subset model** (a subset of the production model, created under the work order — keeps AI-enablement off production), covering `factSalesTransactions` + 14 listed dims + `Calculations > Sales` measures only. 7 activities (question catalogue → build subset model → Prep-for-AI → build/configure agent → validated test+eval+regression baseline → capacity sizing from DEV/trial split → handover). Refined estimate to land the ceiling under 200h: **160–189h** (trimmed top-end padding + the Atomic-composed subset build; protected the Prep-for-AI 45–52 and Test/Eval 30–38 quality core).
- **Decided:** Format = **Pingala SoW** (signable, not a deck/email) via the existing `/fill-sow` pipeline. Offer language **English**. WP 2879 (currency) + WP 2911 (business-unit/vectors) named as **recommended companion work packages, separately scoped** — not folded in; `DimBusinessUnitSegments` out of scope unless WP 2911 lands. Subset model built via the **Pingala Atomic "network/compose" feature** — but that feature is a **skill gap** (not in `pingala-fabric-platform`); kept the mechanism out of the client text, stated only the *requirement* for a separate model. Estimate kept honestly realistic (T&M → actuals bill regardless), ceiling deliberately **sub-200** so it stays under the customer's approval radar — protected by the Change-Request clause + tight Not-in-Scope.
- **Tasks:** none moved. Work sits under `2026-06-15-melbye-data-agent-offer`. User logged the Atomic-feature skill gap as a TODO separately.
- **Next:** **Paused for the user's review of the draft** before finalising. On approval: run `/fill-sow` (needs Melbye full legal name + named Pingala/customer contacts) to render the signable Word doc. Later: document the Atomic compose/"network" feature in `pingala-fabric-platform` (the logged TODO).
- **Next:** live-validate triggering by asking a data-agent question in a fresh session (the optimization loop can't measure it here); decide whether to keep or remove the `_data-agent-eval/` workspace (eval sets reusable if the harness is ever fixed); optionally fold the playbook into the Melbye offer deliverable.

### Session 3 (skill quality review against skill-creator rubric)
- **Did:** Reviewed all **13 non-Microsoft (`bundle: custom`) skills** against the skill-creator writing rubric (read its SKILL.md from the plugin cache; static quality pass, not the eval-loop), one reviewer agent per skill in parallel. Excluded the 28 vendored MS submodule skills. Result: 4 STRONG, 9 SOLID, none NEEDS WORK. Applied **8 correctness/handoff edits across 8 skills** (all uncommitted, working tree): `fabric-project-access` "seven"->"eight" workstreams + Step 2 routes the access-request email through `email-outlook-ready`; `fabric-pipeline-notebook` `<maxResults>` placeholder -> real `MAX_RESULTS` var (was a copy-paste `SyntaxError`); `pingala-project-playbook` Phase 6.1 -> `pingala-visual-identity`; `azure-devops-backlog` Data Excellence epic reconciled; `fabric-data-agent`+`-ops` git/config-as-code ownership split fixed. Captured the Matas data-compare user-stories write-up to TODO.md.
- **Decided:** Data Excellence epic = keep the **Pingala 6-step maturity journey** (Automate->Align->Validate->Adopt->Integrate->Transform) as the Features (original IP, beats the generic 12-dimension MS adoption-roadmap lift); Features in both SKILL.md tree and `push_backlog.py` now mirror the HTML. Git/config-as-code **belongs to `fabric-data-agent`** (where the 10.5 KB reference file already lived and `-ops` already deferred to it) — both frontmatters reworded so serialization mechanics = build skill, capacity/cost/logging/CI-CD-promotion = `-ops`. `skill-creator` plugin is on disk but not invocable this session (no restart) — applied its rubric manually.
- **Tasks:** none in the workspace store. TODO.md: added `2026-06-19 - write up the user stories on matas project data-compare with more detail on the project design etc.`
- **Next:** biggest recurring rubric gap is **deterministic work left un-scripted** — strong `scripts/` candidates: `fabric-rename-entity` (git+JSON rename), `medallion-migration-validation` (QC SQL), `fabric-data-agent-testing` (eval gate), `timestamp-timezone-pipelines`/`fabric-pipeline-notebook` snippets. Also: bundle `pingala-visual-identity` fonts (described-only); tighten `pingala-fabric-platform` over-broad triggers + drop leftover "explore later" scaffolding. Consider committing this session's skill edits.

## 2026-06-22
- **Did:** Took the **Melbye Fabric Data Agent SoW** from first draft to review-ready (`customers/Melbye/data-agent-offer/offer/Melbye_DataAgent_SoW_content.md`, now **v0.7**), plus a one-page **executive brief** (`Melbye_DataAgent_ExecBrief.md`, hours-only, Phase 2 indicative). Major moves: added the **dedicated subset-model** requirement; cut PM overhead + shrank the front-end (workshop->short question-types session; subset build is a scale-down); **restructured into two phases** — Phase 1 *build & validate a proper agent (try it)* vs Phase 2 *production readiness (optional)*; added **publish-to-Teams** to Phase 1 (hedged on EU data-residency sign-off); expanded + de-risked **logging/monitoring** (logging = feasibility-spike + SDK-fronted Delta table, **Purview NOT assumed**, flagged we've never built it). **Verified all Copilot/Teams licensing against live MS Learn** (installed the `microsoft-docs` MCP). Final estimate **Phase 1 106-128h / Phase 2 72-102h** (T&M, hours only — no rate on file).
- **Decided:** Customer is **Swedish (not Danish)** -> EU data-residency is a real decision point (M365 Copilot responses may leave the EU region; fallback = in-region Fabric chat pane). Licensing model confirmed from docs: **M365 Copilot-licensed users = agent usage included/zero-rated** (billing-rates table "No charge" column + footnote 1: B2E, runs under the user's identity, fair-use limits); **unlicensed users = metered Copilot Credits** (free Copilot Chat seat, per-use cost to the org via PAYG/prepaid packs). Both Teams routes (M365 Copilot Agent Store / Copilot Studio) are still **preview**. **F4 is now the Pingala default capacity for data-agent work** (recorded in `fabric-data-agent-ops` via subagent). New standing rule: **bump the doc version on every major revision** (saved as memory `feedback-version-bump-revisions`; demoed across v0.1->v0.7).
- **Tasks:** none moved; work sits under `2026-06-15-melbye-data-agent-offer`.
- **Next:** **Awaiting Niels' review** of SoW v0.7 + exec brief. On approval: `/fill-sow` to render the signable Word doc (needs Melbye full legal name + named Pingala/customer contacts). Open optional: document the Atomic compose/"network" feature in `pingala-fabric-platform` (logged TODO); decide whether to fold the B2E/identity/fair-use licensing nuance into the SoW (currently background only).

### Session 2 (memory system build)
- **Did:** Built and committed (`3f4ccd4`) the unified **Storage/Injection/Recall + skill-evaluation** memory system (Simon-Scrapes model) into one agnostic home at `ops/memory/`. Migrated the 12 Claude-native memories (`~/.claude/.../memory/`) into `ops/memory/store/` with a DB-ready record shape (`id/ts/type/scope/source/tags/status`); old location reduced to a stub. New: `ops/memory/README.md` (tool-neutral spec + by-hand recipes) and `daily/` (raw per-turn stream). **Capture** = `.claude/hooks/capture_turn.py` (Stop hook; per-turn; Haiku summary with a deterministic no-API fallback) — verified against the real transcript. **Injection** = `build_snapshot.py` emits a capped ASCII snapshot via the rewired `session-start.ps1`. **Recall** = keyword grep + cited source. Disabled native auto-memory. Updated `AGENTS.md` (Memory section + Guardrails 7/8), `CLAUDE.md`, `/log`, `/exit`. Designed via a Plan agent after the Ultraplan cloud handoff failed; executed from the approved local plan.
- **Decided:** One agnostic home in `ops/memory/` (markdown = source of truth; any index/DB rebuildable from it). **Recall = keyword now, DB-ready** (semantic deferred until a logged recall-miss). **Capture = per-turn auto-hook** (user's call: all DEV turns are work, so per-turn is signal not noise). **Migrate + hard-disable native auto-memory** via `autoMemoryEnabled: false` — confirmed disablable (corrected my earlier wrong "can't turn it off"). Implementation refinements: capture is one Python script (dodges the PS-5.1 ASCII footgun); snapshot output ASCII-normalized; skill-eval wired into AGENTS.md capability triggers (not `/update-skills`). No duplicate functionality — TODO/tasks/sessions.md/CONTEXT/INBOX untouched; `/log` now also distills `daily/`->`store/`.
- **Tasks:** none in the workspace store.
- **Next:** **Restart Claude Code** to activate the Stop hook + snapshot injection + the auto-memory-disable (hooks load at session start). Then verify: a record auto-lands in `daily/` after the first turn; `/memory` shows disabled; the snapshot appears in the preamble. If the Stop hook doesn't fire, switch the hook's `python` to an absolute interpreter path. Optional later: automate distillation; add SQLite-FTS / vector recall when keyword proves insufficient.

### Session 3 (memory capture fix + local summarizer)
- **Did:** Verified the live memory system after restart - capture/injection/store all firing. Found two flaws in the `daily/` stream: duplicate-**user** records (the `Stop` hook fires several times per turn, each re-grabbing the same last user message) and truncated mid-sentence dumps (no summarizer key in the hook env). Rewrote `capture_turn.py` to **dedupe to one record per `(date,session,user)` turn** (replace-in-place; skip unchanged duplicate Stops) - verified across four synthetic Stop cases. Replaced the Haiku-API summarizer with a **local-only Ollama** call; benchmarked three tiny models on real turns (`qwen2.5:1.5b` distorts facts; `llama3.2:3b` faithful but slower/terser; **`qwen3:1.7b` wins** - faithful, complete, ~5s, `think:false` to dodge the thinking-budget trap). Then **removed all Haiku/API-key code** per the user's call (memory summarization stays fully local/$0). Committed `bec76b0`; gitignored the hook state file; updated `ops/memory/README.md`.
- **Decided:** Memory summarization is **local-only** - no cloud, no API key (avoids separate API billing entirely; an `ANTHROPIC_API_KEY` would also have risked flipping Claude Code off the Max subscription, hence the now-removed dedicated-var guard). Default summarizer model **`qwen3:1.7b`** (override via `MEMORY_SUMMARY_MODEL`); a thinking model must be called with `think:false` or it returns empty. Confirmed the `C:\Dev` workspace repo has **no git remote** - all commits are local-only by design (`/customers/` + `/own/` are separate repos).
- **Tasks:** none moved.
- **Next:** qwen3 summaries land on the next captured turn (capture dedup is already live). Known tradeoff: the `Stop` hook now blocks turn-end ~5s while summarizing - if it grates, switch to write-truncated-now / summarize-async. Untracked: `store/claude-auto-memory-disable.md` (auto-memory note, user's to commit).

### Session 4 (per-project time tracking)
- **Did:** Built a **per-project time-tracking subsystem** as a peer substrate at `ops/time/` (mirrors `ops/memory/`: raw heartbeats -> reviewed timesheets). New: `ops/time/README.md` (spec + by-hand recipe), `rollup.py` (the **15+5 model** engine), `.claude/hooks/track_time.py` (one heartbeat/turn: `UserPromptSubmit` stamps start, `Stop` writes `{ts_start,ts_end,project,session,task}`), `/time` command (live tally + finalize). Wired the daily rollup + review gate into `/log`, and opt-in task tagging into `/task` (start/done writes/clears `ops/time/active-task`). Added `fno_code:` to both project templates, the canonical Identity block, and the task template; **backfilled blank `fno_code:` into all 10 project `CLAUDE.md`s**. Documented in `AGENTS.md` (new *Time tracking* section + folder/identity/structure updates) and the root `CLAUDE.md` harness map. Wrote **ADR-002**. **Self-tested both halves** (rollup: 15-min split, 5-min buffer, 0.25h rounding, catch-up skips today, Dev->INTERNAL-RND non-billable, unknown->UNSET; hook: start-stamp + heartbeat with correct cwd-derived project + task slug). **Gitignored the time data** (`heartbeats/`, `timesheet/`, `active-task`, hook state) — only `README.md` + `rollup.py` stay tracked.
- **Decided:** **ADR-002** — heartbeat + idle-timeout model (**15 min** idle splits an active stretch; **+5 min** tail buffer; round **0.25h**); stale gaps never counted (solves days-long-open sessions). **Attribution strictly by session cwd.** **Numbers stay deterministic — no LLM near billing figures**; the only LLM-suitable piece is the F&O work *description* (reuse existing `ops/memory/daily/` summaries, zero new inference) — **deferred, not built**. F&O codes in each project's `CLAUDE.md` `## Identity` (`fno_code:`), task-level override in the task file; `Dev` (absorbs `ops/` work) -> `INTERNAL-RND` non-billable; missing -> `UNSET`. **Time data is NOT version-controlled** (user's call: operational output regenerable from heartbeats; want the weekly F&O summary, not a git history of hour-by-hour changes). Weekly rollup per project/task = the F&O entry unit; daily finalize = the review gate (correct by editing the timesheet, never the heartbeats).
- **Tasks:** none moved.
- **Next:** Hooks went **live mid-session** (Claude Code picked up the `settings.json` change) — `ops/time/heartbeats/2026-06-22.jsonl` already holds this session's Dev turns from ~13:23 UTC (earlier turns, before the settings edit, are uncaptured); no restart needed, future sessions capture in full. Then **fill real `fno_code:` values** into the 10 projects (all read `UNSET` until then). Optional deferred: the memory-sourced work-description column for F&O comments.

## 2026-06-23
- **Did:** Counted skills in the `pingala` repo at the user's request — **164 `SKILL.md`** under `psi-context-library/` (142 active + 22 archived), none git-tracked in pingala. While answering, flagged heavy duplication in the `skills-for-fabric` vendor submodule (`v0.3.3`, `.claude/vendor/`): 81 SKILL.md files but only ~28 unique, fanned out across plugin folders + a top-level `skills/` dir. Captured a TODO to investigate whether that duplication is intended per-plugin packaging or wasted footprint / load bloat.
- **Decided:** none.
- **Tasks:** none moved. New TODO (2026-06-23): check up on the duplicated skills-for-fabric vendor skills.
- **Next:** triage the duplicated-skills TODO.

## 2026-06-24
*(one session, ran into 06-25)*
- **Did:** From a "the content you write smells of AI" prompt, built a **4-skill family for customer offers**, all hand-grounded and cross-wired. (1) **`writing-voice`** (DA/EN): reverse-engineered Niels's voice from his Danish master's thesis (33k words) + his personal Gmail sent-mail, into a subtraction-first AI-smell remover (em dash = 0 in his own prose, no additive ladder, no meta-narration, thanks-first, first-person ownership) with references `voice-profile`/`business-offers`/`danish`/`english`. (2) **`pingala-offer`**: the trusted-advisor offer doctrine. (3) **`fabric-licensing`** (MS-Learn-cited via the `microsoft-docs` MCP, every fact dated): broad Fabric licensing (capacity SKUs F2-F2048, Free/Pro/PPU, the F64 rule, buy/reserve/pause, Copilot metering); data-agent consumption (Teams, M365 Copilot vs Copilot Credits) folded into references. Ran a **two-build comparison** (skills-on-existing vs skills-from-scratch) that proved the skills can assemble an offer but that the consumption-licensing facts were **single-sourced in a `writing-voice` worked example** (a style sample masquerading as a licensing authority) - which justified building `fabric-licensing`. Finally **consolidated the Melbye SoW** to one canonical **v1.0** (`offer/Melbye_DataAgent_SoW.md`): existing-customer framing (prerequisites Melbye-side only; the model/workspaces/capacity/XMLA are Pingala's, not "Melbye provides"), budget-holder altitude (technical mechanics stripped), licensing at business level, capacity = F4 with no location; **archived 7 variants** to `offer/_archive/` and recorded Melbye's customer-state in its `CONTEXT.md`.
- **Decided:** Offers **target the budget holder, not the technical reader** - cut mechanics, never thin out cost/licensing exposure. **Existing-customer prerequisites never list what Pingala already built or runs.** Capacity is named (F4) but **never its region/placement** in an offer. **Licensing facts live in one cited skill** (`fabric-licensing`, MS-Learn-cited + dated, quarterly re-verify), not in prose; the `business-offers.md` example is demoted to a voice demo that points there. The first cut `fabric-data-agent-licensing` was **overkill -> generalized to `fabric-licensing`** (data-agent consumption became a reference). All doctrine encoded in the skills (see memory `skill-writing-voice` / `skill-pingala-offer` / `skill-fabric-licensing`).
- **Tasks:** none moved in the workspace store; work sits under `2026-06-15-melbye-data-agent-offer`. Melbye canonical SoW **v1.0 is review-ready**.
- **Refined (post-review):** tightened the Melbye SoW v1.0 (cut the "cost picture below is..." meta-framing, the "routine, not a blocker" reassurance, and the vague "Pingala arranges confirmation from Microsoft" promise; **Areas Not in Scope 9 items -> 3**, obvious boilerplate dropped) and **refreshed the `ExecBrief` to v1.0** (business altitude, zero em dashes, existing-customer framing). Folded the lessons into `pingala-offer`: stance rule **"do not promise to find out"**, **"no reassurance / no meta-framing"**, and **"keep Areas Not in Scope short"** (self-check now 9 items).
- **Next:** Niels to review SoW v1.0 + the v1.0 ExecBrief. **Confirm** the "rates via the existing General Terms rate agreement" line (CONTEXT had no rate on file), then `/fill-sow` for the signable Word doc. Optional: live-test the new skills' triggering in a fresh session; commit the session's skill edits (all uncommitted). Time: Melbye `fno_code:` still **UNSET**.

## 2026-06-29
### Session 1 (new-project interview rewrite + Aeven project)
- **Did:** Rewrote `/new-project` to a **strict one-question-per-message interview** (no batching, no `AskUserQuestion` overuse, covers all template fields), and biased the questioning toward **project purpose over project type**. Used it to scaffold a new customer project **`customers/Aeven/ServiceNowPOC`**: a Microsoft Fabric data platform POC that extracts and models **on-premise ServiceNow** data and delivers a Power BI **semantic model** the customer can self-serve reports from; platform live in **Q3, option on Q4**. Created its `CLAUDE.md` + `CONTEXT.md`, opened via Run Task, time tracking active.
- **Decided:** Customer-driven engagement (Aeven approached us). Later integration into Aeven's existing data warehouse is **internal context only — kept out of the offer** so delivery isn't pre-constrained. ServiceNow extraction approach (API vs database) **TBD** given on-prem. Offer language **English**. `fno_code:` left **blank** — created only after the signed offer.
- **Tasks:** new project `customers/Aeven/ServiceNowPOC` scaffolded (offer-drafting work sits under it).
- **Next:** draft the Aeven ServiceNow POC offer (short, value-first); resolve ServiceNow on-prem extraction approach.

### Session 2 (Melbye SoW wording + writing-voice rules)
- **Did:** Wording pass on the **Melbye SoW v1.0** (`offer/Melbye_DataAgent_SoW.md`): rewrote the line 29 overview ("demo agent built in an afternoon / *trustworthy* agent...") into plain formal register ("Setting up a data agent in Fabric takes only a few steps. The real work lies in instructing the agent correctly, validating its answers, getting the metadata in place and correct, and staying in control of both the data and the agent over time."), and replaced **"proper data agent" -> "validated data agent"** in all three places (Phase 1 bullet + two headings). As **Q**, folded the feedback into `writing-voice/SKILL.md`: two new universal rules (**no breezy/"clever" American-sales phrasing** - formal Nordic register; **label/heading words must name the verifiable property, not a vibe** - "validated" not "proper/real/serious") + self-check step 6b.
- **Decided:** The fix belongs in `writing-voice` (register/diction), not `pingala-offer`. Rules encoded so they self-correct on future drafts. Skill edit is live on disk (cascades to all `C:\Dev` projects; body read at invocation, no restart needed).
- **Tasks:** none moved; Melbye work sits under `2026-06-15-melbye-data-agent-offer`.
- **Next:** optionally sweep the other offer SoWs for the same "proper"/"afternoon"-style phrasing; Melbye SoW still pending Niels final sign-off + `/fill-sow`.

### Session 3 (Aeven SoW: resume + brief -> signable SoW)
- **Did:** Resumed the terminated Session-1 thread (reconstructed from `ops/memory/daily/2026-06-29.md` + the on-disk draft). Took `offer/Aeven_ServiceNow_POC_SoW.md` from **v0.1 -> v0.4**. Ran `writing-voice` over the whole document: killed the scene-setting overview opener ("Aeven's operational data lives inside ServiceNow...") into a purpose-first lead, stripped vibe words ("clean", "on its own terms") and the enumerative cadence. Compressed into a **brief-length but signable** SoW: dropped the prose Activities section (the estimates table carries it), restored a lean Organisation section, kept the contractual furniture (Documents/DPA, Delivery, Criteria of Delivery, Signatures). Added via `fabric-project-access`: **Aeven must provision + Power BI Pro-license the Pingala team's in-tenant (`ext_`) accounts** (Licensing bullet + Prerequisite 6). Updated `CONTEXT.md` (state, next actions, decisions log).
- **Decided:** Document is an **actual SoW, kept lean** because it's only ~136-168h ("we can always add on" the optional sections: Purpose, prose Activities, status-reporting). Held to `pingala-offer` stance: zero "?", no decision handed to Aeven (F4 committed; ServiceNow extraction method locked by Pingala in discovery, not asked); budget-holder altitude (no capacity location, no build mechanics); licensing exposure stated in business terms. Q3 scope = Tasks + Activity log + dims (Users/workers, Organisation, Customers, Service structure); **CMDB/assets + SLA deferred to the Q4 option**.
- **Tasks:** none moved; work sits under `customers/Aeven/ServiceNowPOC`.
- **Next:** Niels reviews SoW v0.4. Then gather **Aeven full legal name + named contacts (both sides)** and run `/fill-sow` for the signable Word doc. ServiceNow on-prem extraction approach (API vs DB) still TBD in discovery. `fno_code:` still blank (set after signature).

## 2026-06-30
### DataCompare — source access + vendor-table research
- **Did:** Worked the **DataCompare** source-access picture. (1) Clarified **F&O access**: lead needs **System Administrator** on the F&O-linked Power Platform env (UAT+Prod) for the Link-to-Fabric / Manage Tables flow; noted the **"Matas-configures, we-consume" alternative** (they own the link + table scope, we need only Fabric workspace access) — kept open, not adopted. (2) Worked out **industry-standard access for AX9 + NAV/BC**: per source, request a **transport** (on-prem data gateway → Copy activity for SQL sources; BC→Fabric native link or OData+app-reg for BC SaaS) and a **least-privilege credential** (`db_datareader` on a replica, not live prod). Rewrote **`setup-prerequisites.md` §6** into a per-source transport/credential/owner table + two confirm-with-Matas blockers (NAV/BC on-prem vs cloud; replica availability). (3) Ran **MS-Learn-grounded research on vendor tables** across F&O/AX9/NAV-BC → new standalone task doc **`research/vendor-tables.md`** (cross-system mapping, the 3 coded-field source mappings, F&O Synapse Link extraction gotchas), linked from `CONTEXT.md`.
- **Decided:** **System rollout sequence MFO → AX → NAV**, each compared against **GFO** (the new F&O); drives access prioritisation (recorded in `CONTEXT.md` decisions log + §6). Vendor master is structurally different per family — **F&O normalized (name/address in Global Address Book, join `VendTable.Party`→`DirPartyTable`), AX9 near-flat (`VendTable.Name` + legacy `Address` table), NAV/BC single flat `Vendor` table 23** — so comparison must normalize to canonical shape before diffing.
- **Tasks:** new standalone research task `research/vendor-tables.md` (status in-progress; open Qs: AX9 AOT verify, BC version/localization field IDs, BC tax/posting-group analogues, BC extraction path).
- **Next:** confirm NAV/BC hosting + replica availability with Matas; verify AX9 vendor schema against the real AOT/DB; cross-check the ~29 canonical fields in `extracts/Vendor fields.xlsx` against `VendVendorV2Entity`. (Project still blocked on the Matas capacity decision per email 02.)
- **Note:** `/log` doesn't resolve in DataCompare sessions — commands don't cascade from `C:\Dev\.claude\commands` and this project is its own git root with no `.claude`. This entry written by running the command's steps manually.

## 2026-07-03
### Workspace repo restructure — two-tier backup/DevOps model
- **Did:** Diagnosed why `/log` + workspace skills were missing in some project sessions: command/skill discovery stops at the **git-repo root**, and nested project repos (their own `.git`) never see `C:\Dev\.claude`. Found the deeper problem — the `dev` repo **ignores `/customers/` + `/own/`**, so most projects were backed up by **nothing** (no remote on `C:\Dev` or `DataCompare` either). Designed and built a **two-tier model**: `C:\Dev` = `dev` repo (harness/ops); **one LOCAL repo per customer** (`customers/<Name>`) + one for `own` as the backup/privacy unit; each **project** is then LOCAL (plain folder in the customer repo) or **DEVOPS** (its own repo w/ remote, ignored by the customer repo). Implemented it: archived the `.git` of `DataCompare` + `Data-Discovery` to `C:\Dev\_git-archive\` (history preserved, non-destructive) and folded them into their customer repo; `git init` + initial commit on all 7 units (Aeven, carl-ras, JoeAndTheJuice, Matas, Melbye, Tystofte, own); per-unit `.gitignore` ignoring nested DevOps/wiki sub-repos (Tystofte-Fabric, PowerPortal.wiki, Data-and-AI.wiki), venvs, `.claude`; harness `.claude` junctions relocated to the **customer-repo root** (+ kept on the DevOps sub-repos, with `.claude/` added to their local `.git/info/exclude` so the junction can't leak into a customer's DevOps). Wrote **`ops/bin/heal-repos.ps1`** — idempotent (verified 0-diff on re-run) — to wire any new customer or carved-out code repo in one command.
- **Decided:** **One repo per customer, all LOCAL for now** (no remotes yet — backup lands when a private remote is added per customer). Per-project DevOps-vs-local is decided at code time. Company DevOps repo is **scoped to the code subfolder only** — private notes/context stay at the customer level, never inside a `◆` repo. Folded repos' history archived, not deleted.
- **Tasks:** none in the store moved; this is workspace-infra.
- **Next:** when backup is wanted, add a private remote per customer repo + push (flag: 33 MB `IDA_Atomic_Project_Plan_v2.pptx` in the Matas repo — `.gitignore` big binaries or use LFS before pushing). New customer / carve-to-DevOps → run `ops/bin/heal-repos.ps1`. Optional: delete `C:\Dev\_git-archive\` once satisfied; document the model in `AGENTS.md`.
- **Note:** `/log` now resolves in project sessions again (git root = the customer repo, which carries the `.claude` junction) — takes effect next session per project.

### Carl Ras / Marketo — inbound source re-decided (Event Hub → storage shortcut)
- **Did:** Analysed the sample landing Avro (**5,067 events**) and identified it as **Azure Event Hubs Capture** output (`Microsoft.ServiceBus.Messaging.EventData` envelope; Body = **Stape server-side GTM** payload, `x-ga-*` + `stape_uid`) → chain is **Stape sGTM → Event Hub → Capture Avro → landing storage**. **Proved the enrichment gap:** Impact's account-segment join (`ev_customerSegment*`, `user_id_azure`, `connectedWithApp`) rides **only** the Event Hub feed and is **absent from the GA4 hit** (215 events top-level vs 11 in GA4 user_properties) → GA4→BigQuery would lose it; segment resolves ~4% at **account level**, email almost never. Received Impact's architecture diagram (`data/architecture from impact.png`): ownership settled — **all zones Carl Ras-owned except the Databricks Data Hub (Impact-operated)**, which Fabric replaces. Estimated **Event Hub cost ~€1.2–1.5k/yr** (Standard, 1 TU, Capture-dominated). Built a Pingala-branded **decision one-pager** (`design/gtm-stape-data-pickup.html`, published artifact). Updated `CONTEXT.md` throughout.
- **Decided:** **Keep the Event Hub; consume the landing storage via a Fabric OneLake shortcut (zero-copy); rebuild bronze/silver/gold in Fabric to replace Databricks.** Supersedes the 2026-06-23 greenfield-BigQuery plan — enrichment lives only on the Event Hub path, the capture chain is Carl Ras-owned, and the Event Hub cost is noise while BigQuery is capability-negative (loses the account identity). **Marketo write-back via reverse-ETL (Census/Hightouch)**, not bespoke REST (Marketo replaced 2027; same tool feeds D365). Identity model = **Account as lowest common denominator** (Impact's model, confirmed by the sample).
- **Tasks:** none in the store moved; work sits under `customers/carl-ras/marketo` (tracked via `CONTEXT.md`).
- **Next:** pursue **ownership/access** to the storage + Event Hub setup; confirm the shortcut targets the **raw landing** container (not Impact's bronze/silver); revise `SOLUTION_DESIGN.md` + SoW to **v0.2** (storage-shortcut inbound + reverse-ETL write-back); then Niels review.

### Fix — hooks (memory + time) dead in project sessions since the repo restructure
- **Did:** Diagnosed why `ops/memory/daily/` stopped after 2026-07-02 and time capture went quiet: the Jul-3 restructure wired each unit's `.claude` with junctions for `commands`/`skills` **but not `settings.json`**, so project-rooted sessions (git root = the customer repo) loaded no hooks config → `SessionStart`/`Stop`/`UserPromptSubmit` hooks never fired (memory capture + time heartbeats + session-start snapshot all silent). Hook **scripts** were fine (`capture_turn.py` hardcodes absolute `C:\Dev\ops\...`, derives scope from cwd). Fix: patched `ops/bin/heal-repos.ps1` to also link `settings.json` into every unit — via a **hard link** (file symlinks need admin/Dev-Mode on Windows; hard links don't, same NTFS volume) with a self-heal (relink if content diverges). Re-ran across all 7 units + DevOps sub-repos; verified byte-identical (same hash, `LinkType=HardLink`).
- **Decided:** Harness hooks stay **workspace-scoped** (present in every `C:\Dev` unit, not promoted to global user settings — they write to `C:\Dev\ops`, must not fire in unrelated repos). `settings.json` shared by hard link (single source = `C:\Dev\.claude\settings.json`); `heal-repos.ps1` now wires it for any future customer/carve-out.
- **Next:** Takes effect **next session** (hooks load at startup; expect a one-time hook-trust prompt). Verify: a record lands in `ops/memory/daily/<date>.md` after the first turn and a heartbeat in `ops/time/heartbeats/`. This session's raw capture is still lost but CONTEXT + this log were hand-written, so nothing decision-grade was.

## 2026-07-06
### Harness — customer-isolation model + remote classification
- **Did:** Hardened the two-tier repo model against customer leakage. Established the **two-axis framing**: (1) *internal sharing* — solo now, team-shared repos later, **scoped per customer** (deferred), separate from (2) *customer isolation* — internal info (context/memory/decisions) **never** reaches the customer, always-on. Added **Guardrail 10 (Customer-repo isolation)** to `AGENTS.md`: a customer-facing repo holds only code; internal info is never a child of it (parent/sibling only). Built enforcement into `ops/bin/heal-repos.ps1`: (a) **exclude-guard** — each customer-facing repo local `.git/info/exclude` blocks `.claude/`, `CONTEXT.md`, `CONTEXT_*.md`, `INBOX.md` so a stray internal file cannot be committed to the customer (verified un-committable); (b) **remote classification** — a nested repo origin is matched against a new substrate allowlist `ops/config/internal-remotes.txt` (`github.com/NielsSkytte`, `dev.azure.com/PingalaGlobal`): internal -> ignored only; customer-facing -> ignored + guarded; no remote -> flagged; only customer-facing repos get the guard (fixed the false-positive where the Pingala wiki was guarded); (c) a **safety warning** if a unit repo ever gets a non-internal remote. Also fixed two native-git stderr traps under `$ErrorActionPreference=Stop` (silent `git remote` check; `rev-parse --verify --quiet HEAD`) so `-Only <new customer>` works.
- **Decided:** Repo identity = **.git presence + origin remote**, never the folder name — naming is a human-only convenience and the isolation guard is name-independent. Internal vs customer is decided by **remote org in the URL** (allowlist), not host (github vs dev.azure.com cannot tell them apart — Pingala is also on azure). Allowlist lives in **substrate config**, not the script (Guardrail 7); add a line to onboard a new internal org/teammate.
- **Proposed (NOT yet greenlit / not implemented):** a **storage standard** — projects are folders under the customer; customer-facing code repos nested **inside the project** folder; **role-based folder names** (`fabric/`, `infra/`, `wiki/`) not a single `code/`; multiple code repos per project = multiple role folders; existing flat repos (Tystofte) left as-is. Needs sign-off before `heal-repos` gets **nested (multi-depth) detection** + nested ignore paths.
- **Tasks:** none in the store moved; all work is harness-infra.
- **Next:** decide the storage standard (placement + naming) -> then add nested detection to `heal-repos`. Parked: team-sharing implementation (internal remotes on customer repos + per-customer permissions + M distill-to-team rule). Backup still pending — no remotes yet; handle the 33 MB Matas `.pptx` before a first push.
- **Note:** memory hooks were still dead this session (the settings.json hard-link fix lands next session), so no raw `ops/memory/daily/` capture — this log + AGENTS.md + `heal-repos.ps1`/config are the durable record.

### Harness — two-tier customer/project context model (customer nodes)
- **Did:** Rolled out a **two-tier customer/project context model** across the whole workspace, prompted by "no context when a session opens at `customers/carl-ras` root." Formalised in `AGENTS.md`: new **"Customer nodes"** subsection under *Projects*, updated folder-structure diagram, and a **three-scoped** continuity walk — added a **customer walk** (at `customers/<client>/` with no project: read the node, surface its projects + statuses, prompt which project the session is for, then hand to the project walk) between the workspace walk and the project walk; updated Guardrail 8 and the "referenced customer" + "starting a new project" conventions. Added the customer-walk trigger to root `CLAUDE.md`. Created `_templates/customer/` (CLAUDE.md + CONTEXT.md). **Built customer nodes for all 6 customers** (Aeven, carl-ras, JoeAndTheJuice, Matas, Melbye, Tystofte) — profile + contacts + shared infra + project index, populated from each project's own files (no fabrication). Promoted carl-ras's empty `fabric/` stub to a **placeholder project** (home for today's TODO #1). **Normalised Tystofte** from "umbrella engagement" to a clean customer node and gave its two flat tracks (`Data-Discovery`, `Tystofte-Fabric`) real per-project `CLAUDE.md`+`CONTEXT.md`, splitting the old umbrella content by attribution (shared infra/repos/conventions stayed at the node; per-track state + decisions moved down; two umbrella-level decisions stayed at the node) — no content lost. Earlier in the day captured 5 TODOs (carl-ras Fabric scale-up, Impact/Marketo mail w/ Kasper cc, Aeven ServiceNow PDF offer, Vestforbrænding/Venzo Fabric-capacity follow-up).
- **Decided:** A **customer node = map** (profile, contacts, shared infra, project index), **never a project and never a billing target** — time/tasks attach only to a project (and, for customers, a task within it). **Every** customer gets a node, including single-project ones (index has one row). `own/` stays single-tier. Session start is now three-scoped (workspace root → customer root → project). Tystofte's project-style umbrella identity (`type/scale/fno_code`) was the anti-pattern this removes.
- **Tasks:** none in the store moved; 5 new items captured in `ops/TODO.md` (2026-07-06). No store task promotions.
- **Next:** flesh out the `carl-ras/fabric` placeholder (confirm scope, locate the existing Python model-processing script); confirm the pending Fabric **Update**/pipeline run for `Tystofte-Fabric`; triage the 4 captured business TODOs (Impact mail, Aeven PDF offer, Vestforbrænding follow-up). Isolation unaffected — nodes sit above any future customer-facing sub-repo; `heal-repos.ps1` already excludes internal names there.
- **Note:** memory + time hooks are **firing again** this session (raw stream in `ops/memory/daily/2026-07-06.md`; the settings.json hard-link fix landed as predicted).

### Harness — fabric agent split around the semantic model (roster 5 → 7)
- **Did:** Restructured the agent roster per the AGENTS.md *Fabric scope note*: split `fabric` into **`fabric-back`** (sources → gold: pipelines, notebooks, lakehouses, warehouses, provisioning; `git mv`, history kept), new **`semantic`** (star schema/relationships, DAX measures, Direct Lake vs Import, refresh/processing, VertiPaq optimization, RLS, model docs, Prep for AI, all three data-agent skills), and new **`fabric-front`** (reports/visuals; colleague-owned, handover-oriented). Each agent now lists its **skill loadout** (custom + vendor bundles) and an explicit **scope boundary** (fabric-back hands off at documented gold tables; fabric-front pushes model changes upstream, never report-level workarounds). Found and fixed the reason agents were never invokable: **no YAML frontmatter → never registered as subagent types**; added `name:`+`description:` to all 7 (m, q, content, architect included) — confirmed live registration mid-session, no restart needed. Added a standing **token-discipline** paragraph to every agent (delegate exploration to Explore/general-purpose subagents, parallel fan-out, main context for judgment). Updated: Q's agent-format spec (frontmatter + skills + token discipline now required), M's roster/routing ("wrong number / slow report → start at `semantic`, the contract owner"), cleared the stale `fabric-project-access` Hiring Board entry (skill exists), AGENTS.md roster + scope note (split recorded as done), root CLAUDE.md harness map, `/task` owner list.
- **Decided:** Fabric domain cut in three **at the semantic model** — the model is the contract between backend and frontend and the collaboration surface with the frontend colleague. `semantic` (not `semantic-agent`/`fabric-model`) chosen as the name; rename offer open. Agent frontmatter is now mandatory in Q's format spec.
- **Tasks:** none moved; harness-infra.
- **Next:** attach/verify skills per agent in real work (skill-usage evaluation loop); colleague onboarding onto `semantic`/`fabric-front`; consider `fabric-model` rename if `semantic` reads too broad.

### v1 review — three gates closed, v1.0 tagged (the setup phase ends)
- **Did:** Full infrastructure review (3 parallel audits: `.claude` harness, `ops/` substrate, project config; published as artifact). **Gate 1 (survivable):** private remote `github.com/NielsSkytte/dev-workspace` created + 4 chunked commits pushed (first push since 06-22; 71 dirty paths); `ops/time/` mirrored to OneDrive (`Backup/Dev-ops-time`, wired as /log step); fno_codes filled — Matas `212-01` (Activity REQUIRED on all lines), Carl-Ras/datahub `230-02`, Tystofte/Data-Discovery `4048-1`, Tystofte-Fabric + Aeven marked pending, own/* -> `INTERNAL-RND`; Jul-3 timesheet hand-reconstructed (2.0 h billable Carl-Ras), Jun-23 written off. Renamed `carl-ras`->`Carl-Ras`, `marketo`->`datahub` (folder git mv, identity blocks, node index, CONTEXTs, VS Code task; history untouched). **Gate 2 (drift closed):** ADRs -> `ops/decisions/`; `skill-tree/` + `null` deleted; MasterThesis.pdf -> writing-voice assets; `ops/bin`+`config`+`decisions` mapped in AGENTS.md; `pingala/` sanctioned as shared-repo mount (psi-context-library = first shared skills brain, other Pingala employees contribute); Data-and-AI.wiki declared non-project input; broken refs fixed (task-template agent list, /fill-sow paths, dead PowerPortal task, 3 missing VS Code tasks); Q's skill-vs-command spec corrected to the three-form model; M inventory completed; trackers reconciled (Melbye task -> done, 3 built TODOs checked, off-enum identity fields normalized, 4 idle own/ -> paused); `Move-Item *` permission dropped. **Leak catch:** DevOps-rooted projects keep CLAUDE.md at the repo root — added to the exclude guard + `.project-meta/<sub>/` hard-link shadows give their internal metadata a backup home in the unit repo (git refuses nested-repo paths; verified). **Gate 3 (interview):** evaluation layer COMMITTED (checkpoint at every /log); assisted-triage CANCELLED (superseded by no-double-engineering doctrine + hand triage: 12 stale TODOs -> 6 live, Matas binder task created, Tystofte INBOX package); summarizer kept (`qwen3:1.7b`, 06-22 benchmark stands) with deterministic per-turn sanitizer (charset/injection/bounds; command turns record invocation not help text; tested against the real defect) + new **sentinel** agent (8th) vetting all local-model output at /log. **Tagged `v1.0`, pushed.**
- **Decided:** **Wrap-up commits:** internal repos always auto-commit at /log; DevOps repos always ask (AGENTS.md convention + memory record). **No double-engineering:** DevOps = delivery backlog; `ops/tasks` = thin F&O binder + pre-DevOps motions + own/workspace work. **DevOps-rooted split:** code in DevOps, context in personal repo via `.project-meta` hard links. **Local-model output = untrusted input** (feeds future prompts): deterministic sanitizer + sentinel review before distillation (`ops/memory/README.md` > Output validation). **Fact-only language:** never imply; cite file+line before asserting a defect (memory record + CLAUDE.md). **Freeze rule in force:** no new agent/skill/command without a demonstrated failure.
- **Tasks:** `melbye-data-agent-offer` -> done (retroactive; offer hit v1.0 review-ready 06-24). `task-assisted-triage` -> cancelled. New: `matas-enhance-user-stories` (open; the /switch-task binder — needs Matas Activity + work-item id).
- **Next:** PROJECT WORK (the point of v1). Tystofte session: INBOX package (postgres analysis a la Oracle + design-doc input) + 3-week-old wiki INBOX entry. Matas: activity number(s) from Niels -> fill the binder task. Carl-Ras: Impact mail (Kasper cc), fabric placeholder scope. Aeven: PDF offer. Watch this week: evaluative records accumulating at /log; Friday `/time --month` should show INTERNAL-RND share collapsing. Pending codes: Tystofte-Fabric, Aeven (on signature).

## 2026-07-07
### Aeven — ServiceNow POC offer finalised at v1.0 (session wrapped 2026-07-08)
- **Did:** Processed review feedback (content fine; hours possibly low — accepted, expansion rides the Q4 option / Change Request already in the offer). Bumped the SoW md v0.4 → **v1.0** (history row, content unchanged). Rendered the signable Word doc + PDF from the Pingala template via the /fill-sow toolchain: `ServiceNowPOC/offer/Aeven_ServiceNow_POC_SoW_v1.0.docx` / `.pdf` — 10 pages, TOC updated, verified zero leftover placeholders, zero question marks (pingala-offer gate), Aeven A/S in all 21 Company controls + page header. Inputs: legal name **Aeven A/S** (web-verified, ex-NNIT infra business), Niels Skytte (Pingala; CEO signs), Brøndby/Ballerup, Azure DevOps as task system, docs in English. One deliberate yellow flag: **Aeven day-to-day contact name** (not provided). Fixed three real bugs in the toolchain (`~/.claude/tools/sow_fill.py`): empty-cell corruption, data-bound Company name reverting via `docProps/app.xml`, header literal `<Customer>` — see store record `sow-fill-toolchain`.
- **Decided:** Finalise with estimates unchanged (get started and deliver); optional template sections (Vision & Goal, Criteria for success) left empty rather than yellow-flagged in a signable doc. Both in the project Decisions Log.
- **Tasks:** `2026-07-07-aeven-servicenow-offer-pdf` open → **in-progress** (blocked only on the Aeven contact name).
- **Next:** Get the Aeven contact name → fill the flag → re-export PDF → send (cover email via email-outlook-ready). On signature: F&O code + kick-off. NOTE: a parallel session (own/CapacityManager, WI/AKV/ARM auth findings) ran 07-07/08 and logs itself — its daily records were left for that session to distill. Sentinel agent still not spawnable from a project session (roster registration); distilled records vetted inline.
- **Memory:** distilled `sow-fill-toolchain` (procedural) + `eval-2026-07-07-pingala-offer` (evaluative: pingala-offer fired+helped; /fill-sow recipe needs a Word-open verify step; writing-voice not loaded, borderline).
