# Session Log

Chronological record of workspace sessions — what was done, decided, and what's next. Written at session end (via `/log`, owned by M); the session-start walk reads the latest entry for continuity. **Newest entries at the bottom.**

---

## 2026-06-11
- **Did:** Stood up the ICOR capture→task pipeline. Built `/park` → `ops/TODO.md` (Input) and `/task` → `ops/tasks/` store (Output; open/in-progress/done/cancelled). Created `AGENTS.md` (LLM-agnostic principle) and relocated substrate out of `.claude/` into `ops/`. Renamed the main inbox to `ops/TODO.md` (per-project `INBOX.md` stays the curated knowledge feed via `/brief`). Built the continuity loop: this session log, the session-start walk (SessionStart hook + M routine), and `/log`. Deleted a stray mangled gist file in AtomicCortex.
- **Decided:** ADR-0001 (`own/AtomicCortex`) — AtomicCortex is a *system*, not a project; peer to the personal PKA; "one spec, many instances, federated at seams." LLM-agnostic-substrate / harness-as-accelerator is now Guardrail 7; continuity loop is Guardrail 8.
- **Tasks:** open `2026-06-10-migrate-dev-skills-to-atomiccortex`; in-progress `2026-06-11-complete-llm-agnostic-split` (part 1 substrate relocation done; part 2 = migrate `CLAUDE.md` conventions → `AGENTS.md`).
- **Next:** test the continuity loop in a fresh session; then finish part 2 of the LLM-agnostic split. Optional: extract task-store machinery into the shared spec for AtomicCortex.

## 2026-06-15
- **Did:** Exercised the full capture->triage loop end-to-end. Captured 4 todos; triaged 2 into tasks. Revised `/todo` to a two-beat capture (verbatim-first, then one optional `AskUserQuestion` "sharpen" beat only when a line is too thin to triage — best-guess options + free-text "Other"). Fixed a substrate naming gap: `_TEMPLATE.md` `source:` enum now `todo|inbox|direct` and `/task` step 3 routes a TODO.md capture to `source: todo` with a backlink (previously mis-pointed at `INBOX.md`). Scaffolded placeholder project `customers/Melbye/data-agent-offer` (CLAUDE.md + CONTEXT.md + VS Code task).
- **Decided:** New convention in `AGENTS.md` — *"A referenced customer always has a project"*: a customer named with no folder gets a placeholder project scaffolded immediately (not a full interview, not workspace-level orphan). Wired into `/task` triage. Deliberately NOT mirrored to Claude memory (substrate is the single home; avoids drift — Guardrail 7). Confirmed capture-thinness and triage-readiness are different bars (a line can pass capture yet arrive under-specified at triage — esp. client work, where the fix is a project home, not a harder capture gate).
- **Tasks:** created `2026-06-15-task-assisted-triage` (open, owner Q) and `2026-06-15-melbye-data-agent-offer` (open, owner content, project customers/Melbye/data-agent-offer). Still open in TODO.md: session time-tracking capture (deferred to later triage).
- **Next:** restart a fresh session to verify the continuity loop + revised `/todo` + customer-project rule all fire correctly. Then: build the assisted-triage enhancement (decide trigger: threshold vs daily schedule; routine must land in AGENTS.md). Triage the session-time-tracking todo. Flesh out Melbye via full `/new-project` interview once the deal is real.

### Session 2 (continuity-loop verification)
- **Did:** Verified the continuity loop in a fresh restart — `SessionStart` hook fired correctly and the workspace walk surfaced open work. Clarified two harness behaviours: (1) the hook primes context but does not auto-generate a reply, so the walk appears on the first user message; (2) image paste fails in the VS Code *integrated terminal* (text-only paste) — use the VS Code extension, drag-drop, or a file path. **Finished Part 2 of the LLM-agnostic split:** migrated all operational content (owner, folder structure, buckets, project files/types/scale/identity, agent roster, capability rubric, fabric note, knowledge flow, guardrails 1-9, start/switch process) into `AGENTS.md` as the full operational doc, and rewrote root `CLAUDE.md` as a thin Claude bootstrap (harness map + Claude-only continuity triggers, keeping the cascading project-walk rule). Acid test holds.
- **Decided:** Response-style preference captured to memory — `terse`/`brief`/`full` sticky steering keywords (default `brief`); no edit narration unless asked.
- **Tasks:** `2026-06-11-complete-llm-agnostic-split` → **done** (moved to `ops/tasks/done/`). In-progress queue now empty.
- **Next:** build assisted-triage (`2026-06-15-task-assisted-triage`, owner Q) — decide trigger (threshold vs daily schedule); routine lands in AGENTS.md. Triage the 2 unprocessed TODOs (better email creation; session time-tracking). Flesh out Melbye once the deal is real.

## 2026-06-18
- **Did:** Tystofte project — major Data-Discovery build + rescope. Resumed a stale CONTEXT (last 06-02) against git reality (domains + objekt_kategori already pushed to Fabric `develop`). Discovered and consolidated the Excel handover pipeline into one builder: `build_workbook.py` → the single sheet-protected review workbook `Tystofte_review.xlsx` (sheets Tabeller + Visninger, agreed manual columns), refreshed by a deterministic Python rebuild + keyed merge — verified manual data and row order survive a refresh. Added `explain_views.py` (Claude API, `claude-opus-4-8`) to close the views-refresh gap + a coverage check. **Rescoped Tystofte to an umbrella engagement** (Data-Discovery / Tystofte-Fabric / PowerPortal.wiki; data migration is future/out-of-scope) and split **Data-Discovery** into its own local git repo (moved `_build`, the xlsx, and `obsolete/` out of the wiki; repointed all paths; committed). Moved 11 superseded files to `obsolete/`.
- **Decided:** Review-workbook refresh = Python rebuild + keyed merge, **not** Power Query (openpyxl strips the Fabric DataMashup; views were never a PQ source). Data-Discovery is its own repo. Wiki is read-mostly **input** with **scoped updates only** (never a full sync) since it now holds third-party content. (Full decision log in `customers/Tystofte/CONTEXT.md`.)
- **Tasks:** none in the workspace store — Tystofte is tracked via its own `CONTEXT.md`/`INBOX.md`. Created `customers/Tystofte/INBOX.md` with the wiki scoped-update item (= Tystofte Next Action #1).
- **Next:** define which `PowerPortal.wiki/Handover/` paths Pingala owns + build a safe selective commit/push (never `git add -A` on the wiki); deliver `Tystofte_review.xlsx` to Tystofte for fill-in.

## 2026-06-19
- **Did:** Built an interactive **skill-tree** visualization (`ops/skill-tree/`): a self-contained, fully-offline HTML graph (vendored `vis-network.min.js`) of all 37 skills colored by category (Microsoft / own-Fabric / own-non-Fabric), with existing references (solid) vs proposed/overlap references (dashed), click-to-trace, and full descriptions on hover/click (`skill-data.js`). Reviewed all 10 `bundle: custom` skills for missing cross-references **and** reviewed Microsoft-vs-own for overlap. Applied **8 high-confidence links** as additive "Related skills" sections across 5 custom skills (pipeline trio cross-refs: fabric-pipeline-notebook ↔ timestamp-timezone-pipelines ↔ medallion-migration-validation; playbook → pingala-fabric-platform back-link; pingala-fabric-platform → pingala-visual-identity; own→MS overlap links to spark-operations-cli and e2e-medallion-architecture). Fixed `pingala-fabric-platform`'s 8 **dangling `references/*.md` pointers** (git confirms the dir never existed) by reframing them as an "areas to explore later" roadmap. Diagnosed the `skill-creator` plugin.
- **Decided:** Skill classifier = the `bundle:` frontmatter field (`custom` = our own, `fabric-*`/`powerbi-*` = Microsoft). No true MS↔own duplication — MS skills are CLI *execution*, ours are operational *judgement/methodology*; the fix is to **link**, not merge/delete. Never run skill-creator/generators on the vendored MS skills (submodule — edits clobbered by `/update-skills`); custom skills only. Reference deep-dives stay **inline** (no `references/` split) — reframed as future exploration areas. `skill-creator` is installed + enabled but needs a **full CC restart** (not `/reload-plugins`) to register as an invocable skill.
- **Tasks:** none in the workspace store. TODO.md: added `2026-06-18 — hør Simon om hans claude skills`.
- **Next:** Optionally apply the medium/low-confidence links still shown as dashed in the skill tree (azure-devops-backlog / email-outlook-ready → pingala-visual-identity; medallion → synapse/databricks migration; pingala-fabric-platform → semantic-model / spark / sqldw authoring). Restart Claude Code to make `skill-creator` invocable, then optionally use its eval/variance-analysis feature to test description-triggering accuracy on the custom skills. Follow up on the Simon TODO.

### Session 2 (Fabric Data Agent skills)
- **Did:** Built **3 new custom skills** for building production-grade Microsoft Fabric Data Agents (internal delivery / PoC scope, not sales): `fabric-data-agent` (spine + 4 references: prep-for-ai-vs-agent, instructions-cookbook, ontology-grounding, git-and-config-as-code), `fabric-data-agent-testing`, `fabric-data-agent-ops`; registered in `BUNDLES.md`. Grounded in current MS Learn via parallel research agents, **fact-checked** every baked number against live Learn pages (all 13 held), added a latest-changes pass (GA status, Preview Runtime lifting 25×25, GPT-5.X, MCP server, Foundry/Copilot Studio, SPN runtime preview, Purview query-time enforcement, Assistants→Responses API deadline **26 Aug 2026**), then **enhanced with attributed practitioner war-stories** (3 review agents → Pawar, Webb, Tabular Editor, Ilic, Lachev, Zavarella, Tejwani + MS-confirmed community threads) incl. real CU costs (~5–10k CU/question, not the doc's 400), mapping drift, SDK `thread_url`→`message_url`, no deployment-pipeline rebind. Built them through the real `skill-creator` generator.
- **Decided:** Sales is out of scope — these are the internal delivery playbook (offer is a separate deliverable). `skill-creator`'s **description-optimization loop yields no valid triggering signal on this Windows setup** (nested `claude -p` + command-stub detection reads 0 for all on-topic queries → identical should-not-count scores) and **crashes on emoji in SKILL.md** (cp1252 / byte 0x8f) — so descriptions kept **hand-authored** (pushy, explicit trigger lists, sibling cross-refs); logged to memory as `skill-creator-trigger-eval-limitation`. `/run-skill-generator` ≠ a general generator (it authors app-run skills); the real one is `skill-creator` (needed full CC restart to register).
- **Tasks:** none in the workspace store. Relates to `2026-06-15-melbye-data-agent-offer` (the skills are the reusable backbone under it).

### Session 3 (Melbye data-agent offer draft)
- **Did:** Drafted the **Melbye Fabric Data Agent offer** as Pingala SoW content markdown at `customers/Melbye/data-agent-offer/offer/Melbye_DataAgent_SoW_content.md` (mirrors the Joe & The Juice SoW structure so `/fill-sow` maps cleanly). Grounded the activities/deliverables in the 3 `fabric-data-agent*` skills. Scope = **one** data agent on a **dedicated scoped subset model** (a subset of the production model, created under the work order — keeps AI-enablement off production), covering `factSalesTransactions` + 14 listed dims + `Calculations > Sales` measures only. 7 activities (question catalogue → build subset model → Prep-for-AI → build/configure agent → validated test+eval+regression baseline → capacity sizing from DEV/trial split → handover). Refined estimate to land the ceiling under 200h: **160–189h** (trimmed top-end padding + the Atomic-composed subset build; protected the Prep-for-AI 45–52 and Test/Eval 30–38 quality core).
- **Decided:** Format = **Pingala SoW** (signable, not a deck/email) via the existing `/fill-sow` pipeline. Offer language **English**. WP 2879 (currency) + WP 2911 (business-unit/vectors) named as **recommended companion work packages, separately scoped** — not folded in; `DimBusinessUnitSegments` out of scope unless WP 2911 lands. Subset model built via the **Pingala Atomic "network/compose" feature** — but that feature is a **skill gap** (not in `pingala-fabric-platform`); kept the mechanism out of the client text, stated only the *requirement* for a separate model. Estimate kept honestly realistic (T&M → actuals bill regardless), ceiling deliberately **sub-200** so it stays under the customer's approval radar — protected by the Change-Request clause + tight Not-in-Scope.
- **Tasks:** none moved. Work sits under `2026-06-15-melbye-data-agent-offer`. User logged the Atomic-feature skill gap as a TODO separately.
- **Next:** **Paused for the user's review of the draft** before finalising. On approval: run `/fill-sow` (needs Melbye full legal name + named Pingala/customer contacts) to render the signable Word doc. Later: document the Atomic compose/"network" feature in `pingala-fabric-platform` (the logged TODO).
- **Next:** live-validate triggering by asking a data-agent question in a fresh session (the optimization loop can't measure it here); decide whether to keep or remove the `_data-agent-eval/` workspace (eval sets reusable if the harness is ever fixed); optionally fold the playbook into the Melbye offer deliverable.

### Session 3 (skill quality review against skill-creator rubric)
- **Did:** Reviewed all **13 non-Microsoft (`bundle: custom`) skills** against the skill-creator writing rubric (read its SKILL.md from the plugin cache; static quality pass, not the eval-loop), one reviewer agent per skill in parallel. Excluded the 28 vendored MS submodule skills. Result: 4 STRONG, 9 SOLID, none NEEDS WORK. Applied **8 correctness/handoff edits across 8 skills** (all uncommitted, working tree): `fabric-project-access` "seven"->"eight" workstreams + Step 2 routes the access-request email through `email-outlook-ready`; `fabric-pipeline-notebook` `<maxResults>` placeholder -> real `MAX_RESULTS` var (was a copy-paste `SyntaxError`); `pingala-project-playbook` Phase 6.1 -> `pingala-visual-identity`; `azure-devops-backlog` Data Excellence epic reconciled; `fabric-data-agent`+`-ops` git/config-as-code ownership split fixed. Captured the Matas data-compare user-stories write-up to TODO.md.
- **Decided:** Data Excellence epic = keep the **Pingala 6-step maturity journey** (Automate->Align->Validate->Adopt->Integrate->Transform) as the Features (original IP, beats the generic 12-dimension MS adoption-roadmap lift); Features in both SKILL.md tree and `push_backlog.py` now mirror the HTML. Git/config-as-code **belongs to `fabric-data-agent`** (where the 10.5 KB reference file already lived and `-ops` already deferred to it) — both frontmatters reworded so serialization mechanics = build skill, capacity/cost/logging/CI-CD-promotion = `-ops`. `skill-creator` plugin is on disk but not invocable this session (no restart) — applied its rubric manually.
- **Tasks:** none in the workspace store. TODO.md: added `2026-06-19 - write up the user stories on matas project data-compare with more detail on the project design etc.`
- **Next:** biggest recurring rubric gap is **deterministic work left un-scripted** — strong `scripts/` candidates: `fabric-rename-entity` (git+JSON rename), `medallion-migration-validation` (QC SQL), `fabric-data-agent-testing` (eval gate), `timestamp-timezone-pipelines`/`fabric-pipeline-notebook` snippets. Also: bundle `pingala-visual-identity` fonts (described-only); tighten `pingala-fabric-platform` over-broad triggers + drop leftover "explore later" scaffolding. Consider committing this session's skill edits.

## 2026-06-22
- **Did:** Took the **Melbye Fabric Data Agent SoW** from first draft to review-ready (`customers/Melbye/data-agent-offer/offer/Melbye_DataAgent_SoW_content.md`, now **v0.7**), plus a one-page **executive brief** (`Melbye_DataAgent_ExecBrief.md`, hours-only, Phase 2 indicative). Major moves: added the **dedicated subset-model** requirement; cut PM overhead + shrank the front-end (workshop->short question-types session; subset build is a scale-down); **restructured into two phases** — Phase 1 *build & validate a proper agent (try it)* vs Phase 2 *production readiness (optional)*; added **publish-to-Teams** to Phase 1 (hedged on EU data-residency sign-off); expanded + de-risked **logging/monitoring** (logging = feasibility-spike + SDK-fronted Delta table, **Purview NOT assumed**, flagged we've never built it). **Verified all Copilot/Teams licensing against live MS Learn** (installed the `microsoft-docs` MCP). Final estimate **Phase 1 106-128h / Phase 2 72-102h** (T&M, hours only — no rate on file).
- **Decided:** Customer is **Swedish (not Danish)** -> EU data-residency is a real decision point (M365 Copilot responses may leave the EU region; fallback = in-region Fabric chat pane). Licensing model confirmed from docs: **M365 Copilot-licensed users = agent usage included/zero-rated** (billing-rates table "No charge" column + footnote 1: B2E, runs under the user's identity, fair-use limits); **unlicensed users = metered Copilot Credits** (free Copilot Chat seat, per-use cost to the org via PAYG/prepaid packs). Both Teams routes (M365 Copilot Agent Store / Copilot Studio) are still **preview**. **F4 is now the Pingala default capacity for data-agent work** (recorded in `fabric-data-agent-ops` via subagent). New standing rule: **bump the doc version on every major revision** (saved as memory `feedback-version-bump-revisions`; demoed across v0.1->v0.7).
- **Tasks:** none moved; work sits under `2026-06-15-melbye-data-agent-offer`.
- **Next:** **Awaiting Niels' review** of SoW v0.7 + exec brief. On approval: `/fill-sow` to render the signable Word doc (needs Melbye full legal name + named Pingala/customer contacts). Open optional: document the Atomic compose/"network" feature in `pingala-fabric-platform` (logged TODO); decide whether to fold the B2E/identity/fair-use licensing nuance into the SoW (currently background only).

### Session 2 (memory system build)
- **Did:** Built and committed (`3f4ccd4`) the unified **Storage/Injection/Recall + skill-evaluation** memory system (Simon-Scrapes model) into one agnostic home at `ops/memory/`. Migrated the 12 Claude-native memories (`~/.claude/.../memory/`) into `ops/memory/store/` with a DB-ready record shape (`id/ts/type/scope/source/tags/status`); old location reduced to a stub. New: `ops/memory/README.md` (tool-neutral spec + by-hand recipes) and `daily/` (raw per-turn stream). **Capture** = `.claude/hooks/capture_turn.py` (Stop hook; per-turn; Haiku summary with a deterministic no-API fallback) — verified against the real transcript. **Injection** = `build_snapshot.py` emits a capped ASCII snapshot via the rewired `session-start.ps1`. **Recall** = keyword grep + cited source. Disabled native auto-memory. Updated `AGENTS.md` (Memory section + Guardrails 7/8), `CLAUDE.md`, `/log`, `/exit`. Designed via a Plan agent after the Ultraplan cloud handoff failed; executed from the approved local plan.
- **Decided:** One agnostic home in `ops/memory/` (markdown = source of truth; any index/DB rebuildable from it). **Recall = keyword now, DB-ready** (semantic deferred until a logged recall-miss). **Capture = per-turn auto-hook** (user's call: all DEV turns are work, so per-turn is signal not noise). **Migrate + hard-disable native auto-memory** via `autoMemoryEnabled: false` — confirmed disablable (corrected my earlier wrong "can't turn it off"). Implementation refinements: capture is one Python script (dodges the PS-5.1 ASCII footgun); snapshot output ASCII-normalized; skill-eval wired into AGENTS.md capability triggers (not `/update-skills`). No duplicate functionality — TODO/tasks/sessions.md/CONTEXT/INBOX untouched; `/log` now also distills `daily/`->`store/`.
- **Tasks:** none in the workspace store.
- **Next:** **Restart Claude Code** to activate the Stop hook + snapshot injection + the auto-memory-disable (hooks load at session start). Then verify: a record auto-lands in `daily/` after the first turn; `/memory` shows disabled; the snapshot appears in the preamble. If the Stop hook doesn't fire, switch the hook's `python` to an absolute interpreter path. Optional later: automate distillation; add SQLite-FTS / vector recall when keyword proves insufficient.

### Session 3 (memory capture fix + local summarizer)
- **Did:** Verified the live memory system after restart - capture/injection/store all firing. Found two flaws in the `daily/` stream: duplicate-**user** records (the `Stop` hook fires several times per turn, each re-grabbing the same last user message) and truncated mid-sentence dumps (no summarizer key in the hook env). Rewrote `capture_turn.py` to **dedupe to one record per `(date,session,user)` turn** (replace-in-place; skip unchanged duplicate Stops) - verified across four synthetic Stop cases. Replaced the Haiku-API summarizer with a **local-only Ollama** call; benchmarked three tiny models on real turns (`qwen2.5:1.5b` distorts facts; `llama3.2:3b` faithful but slower/terser; **`qwen3:1.7b` wins** - faithful, complete, ~5s, `think:false` to dodge the thinking-budget trap). Then **removed all Haiku/API-key code** per the user's call (memory summarization stays fully local/$0). Committed `bec76b0`; gitignored the hook state file; updated `ops/memory/README.md`.
- **Decided:** Memory summarization is **local-only** - no cloud, no API key (avoids separate API billing entirely; an `ANTHROPIC_API_KEY` would also have risked flipping Claude Code off the Max subscription, hence the now-removed dedicated-var guard). Default summarizer model **`qwen3:1.7b`** (override via `MEMORY_SUMMARY_MODEL`); a thinking model must be called with `think:false` or it returns empty. Confirmed the `C:\Dev` workspace repo has **no git remote** - all commits are local-only by design (`/customers/` + `/own/` are separate repos).
- **Tasks:** none moved.
- **Next:** qwen3 summaries land on the next captured turn (capture dedup is already live). Known tradeoff: the `Stop` hook now blocks turn-end ~5s while summarizing - if it grates, switch to write-truncated-now / summarize-async. Untracked: `store/claude-auto-memory-disable.md` (auto-memory note, user's to commit).

### Session 4 (per-project time tracking)
- **Did:** Built a **per-project time-tracking subsystem** as a peer substrate at `ops/time/` (mirrors `ops/memory/`: raw heartbeats -> reviewed timesheets). New: `ops/time/README.md` (spec + by-hand recipe), `rollup.py` (the **15+5 model** engine), `.claude/hooks/track_time.py` (one heartbeat/turn: `UserPromptSubmit` stamps start, `Stop` writes `{ts_start,ts_end,project,session,task}`), `/time` command (live tally + finalize). Wired the daily rollup + review gate into `/log`, and opt-in task tagging into `/task` (start/done writes/clears `ops/time/active-task`). Added `fno_code:` to both project templates, the canonical Identity block, and the task template; **backfilled blank `fno_code:` into all 10 project `CLAUDE.md`s**. Documented in `AGENTS.md` (new *Time tracking* section + folder/identity/structure updates) and the root `CLAUDE.md` harness map. Wrote **ADR-002**. **Self-tested both halves** (rollup: 15-min split, 5-min buffer, 0.25h rounding, catch-up skips today, Dev->INTERNAL-RND non-billable, unknown->UNSET; hook: start-stamp + heartbeat with correct cwd-derived project + task slug). **Gitignored the time data** (`heartbeats/`, `timesheet/`, `active-task`, hook state) — only `README.md` + `rollup.py` stay tracked.
- **Decided:** **ADR-002** — heartbeat + idle-timeout model (**15 min** idle splits an active stretch; **+5 min** tail buffer; round **0.25h**); stale gaps never counted (solves days-long-open sessions). **Attribution strictly by session cwd.** **Numbers stay deterministic — no LLM near billing figures**; the only LLM-suitable piece is the F&O work *description* (reuse existing `ops/memory/daily/` summaries, zero new inference) — **deferred, not built**. F&O codes in each project's `CLAUDE.md` `## Identity` (`fno_code:`), task-level override in the task file; `Dev` (absorbs `ops/` work) -> `INTERNAL-RND` non-billable; missing -> `UNSET`. **Time data is NOT version-controlled** (user's call: operational output regenerable from heartbeats; want the weekly F&O summary, not a git history of hour-by-hour changes). Weekly rollup per project/task = the F&O entry unit; daily finalize = the review gate (correct by editing the timesheet, never the heartbeats).
- **Tasks:** none moved.
- **Next:** Hooks went **live mid-session** (Claude Code picked up the `settings.json` change) — `ops/time/heartbeats/2026-06-22.jsonl` already holds this session's Dev turns from ~13:23 UTC (earlier turns, before the settings edit, are uncaptured); no restart needed, future sessions capture in full. Then **fill real `fno_code:` values** into the 10 projects (all read `UNSET` until then). Optional deferred: the memory-sourced work-description column for F&O comments.

## 2026-06-23
- **Did:** Counted skills in the `pingala` repo at the user's request — **164 `SKILL.md`** under `psi-context-library/` (142 active + 22 archived), none git-tracked in pingala. While answering, flagged heavy duplication in the `skills-for-fabric` vendor submodule (`v0.3.3`, `.claude/vendor/`): 81 SKILL.md files but only ~28 unique, fanned out across plugin folders + a top-level `skills/` dir. Captured a TODO to investigate whether that duplication is intended per-plugin packaging or wasted footprint / load bloat.
- **Decided:** none.
- **Tasks:** none moved. New TODO (2026-06-23): check up on the duplicated skills-for-fabric vendor skills.
- **Next:** triage the duplicated-skills TODO.

## 2026-06-24
*(one session, ran into 06-25)*
- **Did:** From a "the content you write smells of AI" prompt, built a **4-skill family for customer offers**, all hand-grounded and cross-wired. (1) **`writing-voice`** (DA/EN): reverse-engineered Niels's voice from his Danish master's thesis (33k words) + his personal Gmail sent-mail, into a subtraction-first AI-smell remover (em dash = 0 in his own prose, no additive ladder, no meta-narration, thanks-first, first-person ownership) with references `voice-profile`/`business-offers`/`danish`/`english`. (2) **`pingala-offer`**: the trusted-advisor offer doctrine. (3) **`fabric-licensing`** (MS-Learn-cited via the `microsoft-docs` MCP, every fact dated): broad Fabric licensing (capacity SKUs F2-F2048, Free/Pro/PPU, the F64 rule, buy/reserve/pause, Copilot metering); data-agent consumption (Teams, M365 Copilot vs Copilot Credits) folded into references. Ran a **two-build comparison** (skills-on-existing vs skills-from-scratch) that proved the skills can assemble an offer but that the consumption-licensing facts were **single-sourced in a `writing-voice` worked example** (a style sample masquerading as a licensing authority) - which justified building `fabric-licensing`. Finally **consolidated the Melbye SoW** to one canonical **v1.0** (`offer/Melbye_DataAgent_SoW.md`): existing-customer framing (prerequisites Melbye-side only; the model/workspaces/capacity/XMLA are Pingala's, not "Melbye provides"), budget-holder altitude (technical mechanics stripped), licensing at business level, capacity = F4 with no location; **archived 7 variants** to `offer/_archive/` and recorded Melbye's customer-state in its `CONTEXT.md`.
- **Decided:** Offers **target the budget holder, not the technical reader** - cut mechanics, never thin out cost/licensing exposure. **Existing-customer prerequisites never list what Pingala already built or runs.** Capacity is named (F4) but **never its region/placement** in an offer. **Licensing facts live in one cited skill** (`fabric-licensing`, MS-Learn-cited + dated, quarterly re-verify), not in prose; the `business-offers.md` example is demoted to a voice demo that points there. The first cut `fabric-data-agent-licensing` was **overkill -> generalized to `fabric-licensing`** (data-agent consumption became a reference). All doctrine encoded in the skills (see memory `skill-writing-voice` / `skill-pingala-offer` / `skill-fabric-licensing`).
- **Tasks:** none moved in the workspace store; work sits under `2026-06-15-melbye-data-agent-offer`. Melbye canonical SoW **v1.0 is review-ready**.
- **Refined (post-review):** tightened the Melbye SoW v1.0 (cut the "cost picture below is..." meta-framing, the "routine, not a blocker" reassurance, and the vague "Pingala arranges confirmation from Microsoft" promise; **Areas Not in Scope 9 items -> 3**, obvious boilerplate dropped) and **refreshed the `ExecBrief` to v1.0** (business altitude, zero em dashes, existing-customer framing). Folded the lessons into `pingala-offer`: stance rule **"do not promise to find out"**, **"no reassurance / no meta-framing"**, and **"keep Areas Not in Scope short"** (self-check now 9 items).
- **Next:** Niels to review SoW v1.0 + the v1.0 ExecBrief. **Confirm** the "rates via the existing General Terms rate agreement" line (CONTEXT had no rate on file), then `/fill-sow` for the signable Word doc. Optional: live-test the new skills' triggering in a fresh session; commit the session's skill edits (all uncommitted). Time: Melbye `fno_code:` still **UNSET**.

## 2026-06-29
### Session 1 (new-project interview rewrite + Aeven project)
- **Did:** Rewrote `/new-project` to a **strict one-question-per-message interview** (no batching, no `AskUserQuestion` overuse, covers all template fields), and biased the questioning toward **project purpose over project type**. Used it to scaffold a new customer project **`customers/Aeven/ServiceNowPOC`**: a Microsoft Fabric data platform POC that extracts and models **on-premise ServiceNow** data and delivers a Power BI **semantic model** the customer can self-serve reports from; platform live in **Q3, option on Q4**. Created its `CLAUDE.md` + `CONTEXT.md`, opened via Run Task, time tracking active.
- **Decided:** Customer-driven engagement (Aeven approached us). Later integration into Aeven's existing data warehouse is **internal context only — kept out of the offer** so delivery isn't pre-constrained. ServiceNow extraction approach (API vs database) **TBD** given on-prem. Offer language **English**. `fno_code:` left **blank** — created only after the signed offer.
- **Tasks:** new project `customers/Aeven/ServiceNowPOC` scaffolded (offer-drafting work sits under it).
- **Next:** draft the Aeven ServiceNow POC offer (short, value-first); resolve ServiceNow on-prem extraction approach.

### Session 2 (Melbye SoW wording + writing-voice rules)
- **Did:** Wording pass on the **Melbye SoW v1.0** (`offer/Melbye_DataAgent_SoW.md`): rewrote the line 29 overview ("demo agent built in an afternoon / *trustworthy* agent...") into plain formal register ("Setting up a data agent in Fabric takes only a few steps. The real work lies in instructing the agent correctly, validating its answers, getting the metadata in place and correct, and staying in control of both the data and the agent over time."), and replaced **"proper data agent" -> "validated data agent"** in all three places (Phase 1 bullet + two headings). As **Q**, folded the feedback into `writing-voice/SKILL.md`: two new universal rules (**no breezy/"clever" American-sales phrasing** - formal Nordic register; **label/heading words must name the verifiable property, not a vibe** - "validated" not "proper/real/serious") + self-check step 6b.
- **Decided:** The fix belongs in `writing-voice` (register/diction), not `pingala-offer`. Rules encoded so they self-correct on future drafts. Skill edit is live on disk (cascades to all `C:\Dev` projects; body read at invocation, no restart needed).
- **Tasks:** none moved; Melbye work sits under `2026-06-15-melbye-data-agent-offer`.
- **Next:** optionally sweep the other offer SoWs for the same "proper"/"afternoon"-style phrasing; Melbye SoW still pending Niels final sign-off + `/fill-sow`.

### Session 3 (Aeven SoW: resume + brief -> signable SoW)
- **Did:** Resumed the terminated Session-1 thread (reconstructed from `ops/memory/daily/2026-06-29.md` + the on-disk draft). Took `offer/Aeven_ServiceNow_POC_SoW.md` from **v0.1 -> v0.4**. Ran `writing-voice` over the whole document: killed the scene-setting overview opener ("Aeven's operational data lives inside ServiceNow...") into a purpose-first lead, stripped vibe words ("clean", "on its own terms") and the enumerative cadence. Compressed into a **brief-length but signable** SoW: dropped the prose Activities section (the estimates table carries it), restored a lean Organisation section, kept the contractual furniture (Documents/DPA, Delivery, Criteria of Delivery, Signatures). Added via `fabric-project-access`: **Aeven must provision + Power BI Pro-license the Pingala team's in-tenant (`ext_`) accounts** (Licensing bullet + Prerequisite 6). Updated `CONTEXT.md` (state, next actions, decisions log).
- **Decided:** Document is an **actual SoW, kept lean** because it's only ~136-168h ("we can always add on" the optional sections: Purpose, prose Activities, status-reporting). Held to `pingala-offer` stance: zero "?", no decision handed to Aeven (F4 committed; ServiceNow extraction method locked by Pingala in discovery, not asked); budget-holder altitude (no capacity location, no build mechanics); licensing exposure stated in business terms. Q3 scope = Tasks + Activity log + dims (Users/workers, Organisation, Customers, Service structure); **CMDB/assets + SLA deferred to the Q4 option**.
- **Tasks:** none moved; work sits under `customers/Aeven/ServiceNowPOC`.
- **Next:** Niels reviews SoW v0.4. Then gather **Aeven full legal name + named contacts (both sides)** and run `/fill-sow` for the signable Word doc. ServiceNow on-prem extraction approach (API vs DB) still TBD in discovery. `fno_code:` still blank (set after signature).

## 2026-06-30
### DataCompare — source access + vendor-table research
- **Did:** Worked the **DataCompare** source-access picture. (1) Clarified **F&O access**: lead needs **System Administrator** on the F&O-linked Power Platform env (UAT+Prod) for the Link-to-Fabric / Manage Tables flow; noted the **"Matas-configures, we-consume" alternative** (they own the link + table scope, we need only Fabric workspace access) — kept open, not adopted. (2) Worked out **industry-standard access for AX9 + NAV/BC**: per source, request a **transport** (on-prem data gateway → Copy activity for SQL sources; BC→Fabric native link or OData+app-reg for BC SaaS) and a **least-privilege credential** (`db_datareader` on a replica, not live prod). Rewrote **`setup-prerequisites.md` §6** into a per-source transport/credential/owner table + two confirm-with-Matas blockers (NAV/BC on-prem vs cloud; replica availability). (3) Ran **MS-Learn-grounded research on vendor tables** across F&O/AX9/NAV-BC → new standalone task doc **`research/vendor-tables.md`** (cross-system mapping, the 3 coded-field source mappings, F&O Synapse Link extraction gotchas), linked from `CONTEXT.md`.
- **Decided:** **System rollout sequence MFO → AX → NAV**, each compared against **GFO** (the new F&O); drives access prioritisation (recorded in `CONTEXT.md` decisions log + §6). Vendor master is structurally different per family — **F&O normalized (name/address in Global Address Book, join `VendTable.Party`→`DirPartyTable`), AX9 near-flat (`VendTable.Name` + legacy `Address` table), NAV/BC single flat `Vendor` table 23** — so comparison must normalize to canonical shape before diffing.
- **Tasks:** new standalone research task `research/vendor-tables.md` (status in-progress; open Qs: AX9 AOT verify, BC version/localization field IDs, BC tax/posting-group analogues, BC extraction path).
- **Next:** confirm NAV/BC hosting + replica availability with Matas; verify AX9 vendor schema against the real AOT/DB; cross-check the ~29 canonical fields in `extracts/Vendor fields.xlsx` against `VendVendorV2Entity`. (Project still blocked on the Matas capacity decision per email 02.)
- **Note:** `/log` doesn't resolve in DataCompare sessions — commands don't cascade from `C:\Dev\.claude\commands` and this project is its own git root with no `.claude`. This entry written by running the command's steps manually.

## 2026-07-03
### Workspace repo restructure — two-tier backup/DevOps model
- **Did:** Diagnosed why `/log` + workspace skills were missing in some project sessions: command/skill discovery stops at the **git-repo root**, and nested project repos (their own `.git`) never see `C:\Dev\.claude`. Found the deeper problem — the `dev` repo **ignores `/customers/` + `/own/`**, so most projects were backed up by **nothing** (no remote on `C:\Dev` or `DataCompare` either). Designed and built a **two-tier model**: `C:\Dev` = `dev` repo (harness/ops); **one LOCAL repo per customer** (`customers/<Name>`) + one for `own` as the backup/privacy unit; each **project** is then LOCAL (plain folder in the customer repo) or **DEVOPS** (its own repo w/ remote, ignored by the customer repo). Implemented it: archived the `.git` of `DataCompare` + `Data-Discovery` to `C:\Dev\_git-archive\` (history preserved, non-destructive) and folded them into their customer repo; `git init` + initial commit on all 7 units (Aeven, carl-ras, JoeAndTheJuice, Matas, Melbye, Tystofte, own); per-unit `.gitignore` ignoring nested DevOps/wiki sub-repos (Tystofte-Fabric, PowerPortal.wiki, Data-and-AI.wiki), venvs, `.claude`; harness `.claude` junctions relocated to the **customer-repo root** (+ kept on the DevOps sub-repos, with `.claude/` added to their local `.git/info/exclude` so the junction can't leak into a customer's DevOps). Wrote **`ops/bin/heal-repos.ps1`** — idempotent (verified 0-diff on re-run) — to wire any new customer or carved-out code repo in one command.
- **Decided:** **One repo per customer, all LOCAL for now** (no remotes yet — backup lands when a private remote is added per customer). Per-project DevOps-vs-local is decided at code time. Company DevOps repo is **scoped to the code subfolder only** — private notes/context stay at the customer level, never inside a `◆` repo. Folded repos' history archived, not deleted.
- **Tasks:** none in the store moved; this is workspace-infra.
- **Next:** when backup is wanted, add a private remote per customer repo + push (flag: 33 MB `IDA_Atomic_Project_Plan_v2.pptx` in the Matas repo — `.gitignore` big binaries or use LFS before pushing). New customer / carve-to-DevOps → run `ops/bin/heal-repos.ps1`. Optional: delete `C:\Dev\_git-archive\` once satisfied; document the model in `AGENTS.md`.
- **Note:** `/log` now resolves in project sessions again (git root = the customer repo, which carries the `.claude` junction) — takes effect next session per project.

### Carl Ras / Marketo — inbound source re-decided (Event Hub → storage shortcut)
- **Did:** Analysed the sample landing Avro (**5,067 events**) and identified it as **Azure Event Hubs Capture** output (`Microsoft.ServiceBus.Messaging.EventData` envelope; Body = **Stape server-side GTM** payload, `x-ga-*` + `stape_uid`) → chain is **Stape sGTM → Event Hub → Capture Avro → landing storage**. **Proved the enrichment gap:** Impact's account-segment join (`ev_customerSegment*`, `user_id_azure`, `connectedWithApp`) rides **only** the Event Hub feed and is **absent from the GA4 hit** (215 events top-level vs 11 in GA4 user_properties) → GA4→BigQuery would lose it; segment resolves ~4% at **account level**, email almost never. Received Impact's architecture diagram (`data/architecture from impact.png`): ownership settled — **all zones Carl Ras-owned except the Databricks Data Hub (Impact-operated)**, which Fabric replaces. Estimated **Event Hub cost ~€1.2–1.5k/yr** (Standard, 1 TU, Capture-dominated). Built a Pingala-branded **decision one-pager** (`design/gtm-stape-data-pickup.html`, published artifact). Updated `CONTEXT.md` throughout.
- **Decided:** **Keep the Event Hub; consume the landing storage via a Fabric OneLake shortcut (zero-copy); rebuild bronze/silver/gold in Fabric to replace Databricks.** Supersedes the 2026-06-23 greenfield-BigQuery plan — enrichment lives only on the Event Hub path, the capture chain is Carl Ras-owned, and the Event Hub cost is noise while BigQuery is capability-negative (loses the account identity). **Marketo write-back via reverse-ETL (Census/Hightouch)**, not bespoke REST (Marketo replaced 2027; same tool feeds D365). Identity model = **Account as lowest common denominator** (Impact's model, confirmed by the sample).
- **Tasks:** none in the store moved; work sits under `customers/carl-ras/marketo` (tracked via `CONTEXT.md`).
- **Next:** pursue **ownership/access** to the storage + Event Hub setup; confirm the shortcut targets the **raw landing** container (not Impact's bronze/silver); revise `SOLUTION_DESIGN.md` + SoW to **v0.2** (storage-shortcut inbound + reverse-ETL write-back); then Niels review.

### Fix — hooks (memory + time) dead in project sessions since the repo restructure
- **Did:** Diagnosed why `ops/memory/daily/` stopped after 2026-07-02 and time capture went quiet: the Jul-3 restructure wired each unit's `.claude` with junctions for `commands`/`skills` **but not `settings.json`**, so project-rooted sessions (git root = the customer repo) loaded no hooks config → `SessionStart`/`Stop`/`UserPromptSubmit` hooks never fired (memory capture + time heartbeats + session-start snapshot all silent). Hook **scripts** were fine (`capture_turn.py` hardcodes absolute `C:\Dev\ops\...`, derives scope from cwd). Fix: patched `ops/bin/heal-repos.ps1` to also link `settings.json` into every unit — via a **hard link** (file symlinks need admin/Dev-Mode on Windows; hard links don't, same NTFS volume) with a self-heal (relink if content diverges). Re-ran across all 7 units + DevOps sub-repos; verified byte-identical (same hash, `LinkType=HardLink`).
- **Decided:** Harness hooks stay **workspace-scoped** (present in every `C:\Dev` unit, not promoted to global user settings — they write to `C:\Dev\ops`, must not fire in unrelated repos). `settings.json` shared by hard link (single source = `C:\Dev\.claude\settings.json`); `heal-repos.ps1` now wires it for any future customer/carve-out.
- **Next:** Takes effect **next session** (hooks load at startup; expect a one-time hook-trust prompt). Verify: a record lands in `ops/memory/daily/<date>.md` after the first turn and a heartbeat in `ops/time/heartbeats/`. This session's raw capture is still lost but CONTEXT + this log were hand-written, so nothing decision-grade was.

## 2026-07-06
### Harness — customer-isolation model + remote classification
- **Did:** Hardened the two-tier repo model against customer leakage. Established the **two-axis framing**: (1) *internal sharing* — solo now, team-shared repos later, **scoped per customer** (deferred), separate from (2) *customer isolation* — internal info (context/memory/decisions) **never** reaches the customer, always-on. Added **Guardrail 10 (Customer-repo isolation)** to `AGENTS.md`: a customer-facing repo holds only code; internal info is never a child of it (parent/sibling only). Built enforcement into `ops/bin/heal-repos.ps1`: (a) **exclude-guard** — each customer-facing repo local `.git/info/exclude` blocks `.claude/`, `CONTEXT.md`, `CONTEXT_*.md`, `INBOX.md` so a stray internal file cannot be committed to the customer (verified un-committable); (b) **remote classification** — a nested repo origin is matched against a new substrate allowlist `ops/config/internal-remotes.txt` (`github.com/NielsSkytte`, `dev.azure.com/PingalaGlobal`): internal -> ignored only; customer-facing -> ignored + guarded; no remote -> flagged; only customer-facing repos get the guard (fixed the false-positive where the Pingala wiki was guarded); (c) a **safety warning** if a unit repo ever gets a non-internal remote. Also fixed two native-git stderr traps under `$ErrorActionPreference=Stop` (silent `git remote` check; `rev-parse --verify --quiet HEAD`) so `-Only <new customer>` works.
- **Decided:** Repo identity = **.git presence + origin remote**, never the folder name — naming is a human-only convenience and the isolation guard is name-independent. Internal vs customer is decided by **remote org in the URL** (allowlist), not host (github vs dev.azure.com cannot tell them apart — Pingala is also on azure). Allowlist lives in **substrate config**, not the script (Guardrail 7); add a line to onboard a new internal org/teammate.
- **Proposed (NOT yet greenlit / not implemented):** a **storage standard** — projects are folders under the customer; customer-facing code repos nested **inside the project** folder; **role-based folder names** (`fabric/`, `infra/`, `wiki/`) not a single `code/`; multiple code repos per project = multiple role folders; existing flat repos (Tystofte) left as-is. Needs sign-off before `heal-repos` gets **nested (multi-depth) detection** + nested ignore paths.
- **Tasks:** none in the store moved; all work is harness-infra.
- **Next:** decide the storage standard (placement + naming) -> then add nested detection to `heal-repos`. Parked: team-sharing implementation (internal remotes on customer repos + per-customer permissions + M distill-to-team rule). Backup still pending — no remotes yet; handle the 33 MB Matas `.pptx` before a first push.
- **Note:** memory hooks were still dead this session (the settings.json hard-link fix lands next session), so no raw `ops/memory/daily/` capture — this log + AGENTS.md + `heal-repos.ps1`/config are the durable record.

### Harness — two-tier customer/project context model (customer nodes)
- **Did:** Rolled out a **two-tier customer/project context model** across the whole workspace, prompted by "no context when a session opens at `customers/carl-ras` root." Formalised in `AGENTS.md`: new **"Customer nodes"** subsection under *Projects*, updated folder-structure diagram, and a **three-scoped** continuity walk — added a **customer walk** (at `customers/<client>/` with no project: read the node, surface its projects + statuses, prompt which project the session is for, then hand to the project walk) between the workspace walk and the project walk; updated Guardrail 8 and the "referenced customer" + "starting a new project" conventions. Added the customer-walk trigger to root `CLAUDE.md`. Created `_templates/customer/` (CLAUDE.md + CONTEXT.md). **Built customer nodes for all 6 customers** (Aeven, carl-ras, JoeAndTheJuice, Matas, Melbye, Tystofte) — profile + contacts + shared infra + project index, populated from each project's own files (no fabrication). Promoted carl-ras's empty `fabric/` stub to a **placeholder project** (home for today's TODO #1). **Normalised Tystofte** from "umbrella engagement" to a clean customer node and gave its two flat tracks (`Data-Discovery`, `Tystofte-Fabric`) real per-project `CLAUDE.md`+`CONTEXT.md`, splitting the old umbrella content by attribution (shared infra/repos/conventions stayed at the node; per-track state + decisions moved down; two umbrella-level decisions stayed at the node) — no content lost. Earlier in the day captured 5 TODOs (carl-ras Fabric scale-up, Impact/Marketo mail w/ Kasper cc, Aeven ServiceNow PDF offer, Vestforbrænding/Venzo Fabric-capacity follow-up).
- **Decided:** A **customer node = map** (profile, contacts, shared infra, project index), **never a project and never a billing target** — time/tasks attach only to a project (and, for customers, a task within it). **Every** customer gets a node, including single-project ones (index has one row). `own/` stays single-tier. Session start is now three-scoped (workspace root → customer root → project). Tystofte's project-style umbrella identity (`type/scale/fno_code`) was the anti-pattern this removes.
- **Tasks:** none in the store moved; 5 new items captured in `ops/TODO.md` (2026-07-06). No store task promotions.
- **Next:** flesh out the `carl-ras/fabric` placeholder (confirm scope, locate the existing Python model-processing script); confirm the pending Fabric **Update**/pipeline run for `Tystofte-Fabric`; triage the 4 captured business TODOs (Impact mail, Aeven PDF offer, Vestforbrænding follow-up). Isolation unaffected — nodes sit above any future customer-facing sub-repo; `heal-repos.ps1` already excludes internal names there.
- **Note:** memory + time hooks are **firing again** this session (raw stream in `ops/memory/daily/2026-07-06.md`; the settings.json hard-link fix landed as predicted).

### Harness — fabric agent split around the semantic model (roster 5 → 7)
- **Did:** Restructured the agent roster per the AGENTS.md *Fabric scope note*: split `fabric` into **`fabric-back`** (sources → gold: pipelines, notebooks, lakehouses, warehouses, provisioning; `git mv`, history kept), new **`semantic`** (star schema/relationships, DAX measures, Direct Lake vs Import, refresh/processing, VertiPaq optimization, RLS, model docs, Prep for AI, all three data-agent skills), and new **`fabric-front`** (reports/visuals; colleague-owned, handover-oriented). Each agent now lists its **skill loadout** (custom + vendor bundles) and an explicit **scope boundary** (fabric-back hands off at documented gold tables; fabric-front pushes model changes upstream, never report-level workarounds). Found and fixed the reason agents were never invokable: **no YAML frontmatter → never registered as subagent types**; added `name:`+`description:` to all 7 (m, q, content, architect included) — confirmed live registration mid-session, no restart needed. Added a standing **token-discipline** paragraph to every agent (delegate exploration to Explore/general-purpose subagents, parallel fan-out, main context for judgment). Updated: Q's agent-format spec (frontmatter + skills + token discipline now required), M's roster/routing ("wrong number / slow report → start at `semantic`, the contract owner"), cleared the stale `fabric-project-access` Hiring Board entry (skill exists), AGENTS.md roster + scope note (split recorded as done), root CLAUDE.md harness map, `/task` owner list.
- **Decided:** Fabric domain cut in three **at the semantic model** — the model is the contract between backend and frontend and the collaboration surface with the frontend colleague. `semantic` (not `semantic-agent`/`fabric-model`) chosen as the name; rename offer open. Agent frontmatter is now mandatory in Q's format spec.
- **Tasks:** none moved; harness-infra.
- **Next:** attach/verify skills per agent in real work (skill-usage evaluation loop); colleague onboarding onto `semantic`/`fabric-front`; consider `fabric-model` rename if `semantic` reads too broad.

### v1 review — three gates closed, v1.0 tagged (the setup phase ends)
- **Did:** Full infrastructure review (3 parallel audits: `.claude` harness, `ops/` substrate, project config; published as artifact). **Gate 1 (survivable):** private remote `github.com/NielsSkytte/dev-workspace` created + 4 chunked commits pushed (first push since 06-22; 71 dirty paths); `ops/time/` mirrored to OneDrive (`Backup/Dev-ops-time`, wired as /log step); fno_codes filled — Matas `212-01` (Activity REQUIRED on all lines), Carl-Ras/datahub `230-02`, Tystofte/Data-Discovery `4048-1`, Tystofte-Fabric + Aeven marked pending, own/* -> `INTERNAL-RND`; Jul-3 timesheet hand-reconstructed (2.0 h billable Carl-Ras), Jun-23 written off. Renamed `carl-ras`->`Carl-Ras`, `marketo`->`datahub` (folder git mv, identity blocks, node index, CONTEXTs, VS Code task; history untouched). **Gate 2 (drift closed):** ADRs -> `ops/decisions/`; `skill-tree/` + `null` deleted; MasterThesis.pdf -> writing-voice assets; `ops/bin`+`config`+`decisions` mapped in AGENTS.md; `pingala/` sanctioned as shared-repo mount (psi-context-library = first shared skills brain, other Pingala employees contribute); Data-and-AI.wiki declared non-project input; broken refs fixed (task-template agent list, /fill-sow paths, dead PowerPortal task, 3 missing VS Code tasks); Q's skill-vs-command spec corrected to the three-form model; M inventory completed; trackers reconciled (Melbye task -> done, 3 built TODOs checked, off-enum identity fields normalized, 4 idle own/ -> paused); `Move-Item *` permission dropped. **Leak catch:** DevOps-rooted projects keep CLAUDE.md at the repo root — added to the exclude guard + `.project-meta/<sub>/` hard-link shadows give their internal metadata a backup home in the unit repo (git refuses nested-repo paths; verified). **Gate 3 (interview):** evaluation layer COMMITTED (checkpoint at every /log); assisted-triage CANCELLED (superseded by no-double-engineering doctrine + hand triage: 12 stale TODOs -> 6 live, Matas binder task created, Tystofte INBOX package); summarizer kept (`qwen3:1.7b`, 06-22 benchmark stands) with deterministic per-turn sanitizer (charset/injection/bounds; command turns record invocation not help text; tested against the real defect) + new **sentinel** agent (8th) vetting all local-model output at /log. **Tagged `v1.0`, pushed.**
- **Decided:** **Wrap-up commits:** internal repos always auto-commit at /log; DevOps repos always ask (AGENTS.md convention + memory record). **No double-engineering:** DevOps = delivery backlog; `ops/tasks` = thin F&O binder + pre-DevOps motions + own/workspace work. **DevOps-rooted split:** code in DevOps, context in personal repo via `.project-meta` hard links. **Local-model output = untrusted input** (feeds future prompts): deterministic sanitizer + sentinel review before distillation (`ops/memory/README.md` > Output validation). **Fact-only language:** never imply; cite file+line before asserting a defect (memory record + CLAUDE.md). **Freeze rule in force:** no new agent/skill/command without a demonstrated failure.
- **Tasks:** `melbye-data-agent-offer` -> done (retroactive; offer hit v1.0 review-ready 06-24). `task-assisted-triage` -> cancelled. New: `matas-enhance-user-stories` (open; the /switch-task binder — needs Matas Activity + work-item id).
- **Next:** PROJECT WORK (the point of v1). Tystofte session: INBOX package (postgres analysis a la Oracle + design-doc input) + 3-week-old wiki INBOX entry. Matas: activity number(s) from Niels -> fill the binder task. Carl-Ras: Impact mail (Kasper cc), fabric placeholder scope. Aeven: PDF offer. Watch this week: evaluative records accumulating at /log; Friday `/time --month` should show INTERNAL-RND share collapsing. Pending codes: Tystofte-Fabric, Aeven (on signature).

## 2026-07-07
### Aeven — ServiceNow POC offer finalised at v1.0 (session wrapped 2026-07-08)
- **Did:** Processed review feedback (content fine; hours possibly low — accepted, expansion rides the Q4 option / Change Request already in the offer). Bumped the SoW md v0.4 → **v1.0** (history row, content unchanged). Rendered the signable Word doc + PDF from the Pingala template via the /fill-sow toolchain: `ServiceNowPOC/offer/Aeven_ServiceNow_POC_SoW_v1.0.docx` / `.pdf` — 10 pages, TOC updated, verified zero leftover placeholders, zero question marks (pingala-offer gate), Aeven A/S in all 21 Company controls + page header. Inputs: legal name **Aeven A/S** (web-verified, ex-NNIT infra business), Niels Skytte (Pingala; CEO signs), Brøndby/Ballerup, Azure DevOps as task system, docs in English. One deliberate yellow flag: **Aeven day-to-day contact name** (not provided). Fixed three real bugs in the toolchain (`~/.claude/tools/sow_fill.py`): empty-cell corruption, data-bound Company name reverting via `docProps/app.xml`, header literal `<Customer>` — see store record `sow-fill-toolchain`.
- **Decided:** Finalise with estimates unchanged (get started and deliver); optional template sections (Vision & Goal, Criteria for success) left empty rather than yellow-flagged in a signable doc. Both in the project Decisions Log.
- **Tasks:** `2026-07-07-aeven-servicenow-offer-pdf` open → **in-progress** (blocked only on the Aeven contact name).
- **Next:** Get the Aeven contact name → fill the flag → re-export PDF → send (cover email via email-outlook-ready). On signature: F&O code + kick-off. NOTE: a parallel session (own/CapacityManager, WI/AKV/ARM auth findings) ran 07-07/08 and logs itself — its daily records were left for that session to distill. Sentinel agent still not spawnable from a project session (roster registration); distilled records vetted inline.
- **Memory:** distilled `sow-fill-toolchain` (procedural) + `eval-2026-07-07-pingala-offer` (evaluative: pingala-offer fired+helped; /fill-sow recipe needs a Word-open verify step; writing-voice not loaded, borderline).

## 2026-07-20
### Vestforbrænding — Dataverse "Link to Fabric" blocker root-caused and resolved (capacity Contributor)
- **Did:** Diagnosed why existing workspaces (on the fckursdev F8 / North Europe capacity) never appeared in the Dataverse *Link to Microsoft Fabric* workspace picker. Worked the problem live in-browser (make.powerapps.com wizard) and via the Power BI REST *Try it* console. The decisive split: `GET api.powerbi.com/v1.0/myorg/groups` **returned** both workspaces for svc_CRMSandbox while the wizard showed only "Create new workspace" — isolating it to a capacity-permission gap, not visibility/licensing/region/network/cache. Ruled out, in order: capacity type/region (F8 NE confirmed), tenant/guest, propagation lag (days old), already-linked workspaces (new/empty), and the service-principal tenant-setting theory. Root cause confirmed empirically by Niels: **svc_CRMSandbox lacked Contributor on the capacity set in the Fabric Admin Portal** (Capacity settings → Contributor permissions). Key trap surfaced: **Azure-portal RBAC Contributor on the `Microsoft.Fabric/capacities` resource did nothing** — only the Fabric-admin-portal capacity Contributor governs Link to Fabric; reaching that setting needed the Fabric admin PIM role. Two in-product tells that finally pointed the way: the workspace banner "This workspace is configured to use a capacity you don't have access to" and greyed-out capacity-scoped settings (large/small semantic model). Retrospective doc check: the capacity-permission requirement is **not** documented for the existing-workspace path (only for create-new); the wizard's "Access to Power BI and Fabric capacity" validation even false-passes. Also worked out the least-privilege grant path: capacity **Admin** (not tenant Fabric admin) can add contributors; Azure RBAC ≠ Fabric capacity admin.
- **Decided:** Standard Pingala customer ask going forward = request capacity **Admin** for `SG-Fabric-Capacity-Admin` on the project capacity (scoped to one capacity, not tenant-wide), performed by anyone holding capacity Admin — no tenant Fabric admin needed either side; drop to Contributor only if governance insists. Azure-portal capacity RBAC is explicitly **not** a substitute and must be called out in the request.
- **Tasks:** `2026-07-07-vestforbraending-venzo-capacity-followup` open → **done** (blocker resolved).
- **Next:** Prospect-stage; no open motion on Vestforbrænding. Fold the same capacity-Contributor check into the customer access-request workflow when the next Fabric engagement kicks off.
- **Memory:** captured as `fabric-project-access` skill §8.5 (Fabric Capacity Permissions — two layers) with symptoms, `/groups` isolation test, fix, least-privilege grant path, and six MS Learn citations; skill triggers extended to fire on Link-to-Fabric access problems. Customer CONTEXT.md marked RESOLVED. Commits: `90056c6` (§8.5 + task close), `78c5509` (Vestforbrænding CONTEXT.md, nested repo), `384d2b8` (§8.5 least-privilege addition).

## 2026-07-23
### Element Logic — LineageDocumentation: project scaffolded, lineage engine built, verified, viewer iterated to review-ready (session ran 07-20 → 07-23)
- **Did:** Scaffolded new customer node `customers/ElementLogic` + project `LineageDocumentation` (fno 6001-01, task 45394) via /new-project. Surveyed the delivered Atomic repo (`Input/Pingala Fabric ETL/`, 525 SQL files). **Phase 0** (6-agent research workflow): parser bake-off on real repo SQL (sqlglot 30.13 wins; SQLLineage collapses on the universal `SELECT * FROM Entity` pattern), SQL/ingestion censuses (zero MERGE/INSERT-SELECT anywhere; every layer hop = dynamic CTAS proc with name identity), Atomic-convention mapping (15 assumptions confirmed by Niels), accelerator verdicts (fabric-toolbox Lineage_Extractor + sempy-labs rejected with grounds). Architecture proposal approved → **built the engine** (`lineage_engine/`): static full-repo run = 19,996 nodes / 18,279 edges / 15,983 column edges (97.8% full, 0 failures), canonical nodes+edges schema (JSON↔Parquet 1:1), online Fabric-notebook mode (catalog + strict-superset enrichment, fake-catalog tested; not yet live), `lineage_engine.query` CLI (`--json`). **Adversarial verification** (4 agents vs raw SQL): 14/14 sampled edges real, never-fabricate audit clean; one label-granularity limitation documented. **HTML viewer** shipped + 6 usability rounds with Niels: filter-to-cone on select, column trace with per-hop transformation steps, step-back hierarchy (trace→cone→all, Esc-only clearing), side-by-side view/table pairs with column-mapping cardinality (1:1/1:N/N:1/N:M), per-table exclude, hub badges, structural reference-table detection (portable: degree≥20 + raw/util + ≤3 cols + fan/col≥12 — replaced a hardcoded list after Niels caught the portability problem), dataviz-validated Pingala-derived palette (light+dark). **Security find:** SP client secret committed in `NB_Manage_Shortcut_LZ_To_Raw` — customer must rotate.
- **Decided:** (all in project CONTEXT.md Decisions Log, 16 entries) sqlglot core; Prod valueSet; English UI; option 3 for transformation display (per-hop steps, no embedded view SQL); semantic-model business-language metadata parked as next phase, generated FROM the lineage store; reference tables detected structurally with per-installation override, never hardcoded.
- **Tasks:** none in ops/tasks (F&O binder is fno 6001-01 / task 45394 directly).
- **Next:** customer rotates the SP secret; run `NB_Lineage_Online.py` in the tenant (raw-layer gaps + Parquet); decide viewer publishing channel; next phase = semantic-model node type + TMDL column descriptions. **INFRA:** this session (rooted at `C:\Dev\customers`) got NO capture/time hooks — Element Logic hours 07-20..23 need manual timesheet entry; hook scoping needs a fix (store record `hooks-subdir-session-gap`).
- **Memory:** sentinel vetted daily/2026-07-22 (1 flag: dropped-qualifier billing claim, not distilled; other record clean, already README-covered). Distilled: `atomic-lineage-engine` (reference), `pingala-palette-dataviz-conflict` (reference), `hooks-subdir-session-gap` (project/infra), `eval-2026-07-22-lineage-viewer` (evaluative: dataviz + pingala-visual-identity fired+helped; pingala-fabric-platform gap — layer-ID conventions undocumented, candidate skill update).

### Carl Ras / fabric - CapacityManager: auth chain verified end-to-end, blocked on regional CU quota (session ran 07-17, wrapped 07-23)
- **Did:** Continued the CapacityManager deployment test loop after the customer disabled the Key Vault firewall (clearing the 07-15 `ForbiddenByFirewall` blocker). Three test iterations, each unblocked by surfacing the next hidden error body: (1) SP token 400 -> patched `NB_CapacityManager_Bootstrap` to raise with the AAD response body (commit `bcd0267`); (2) revealed AADSTS700016 -> wrong app id stored in `scale-sp-id`, customer corrected it in AKV; (3) ARM 400 on the scale PATCH -> extended body-surfacing to all ARM/PBI calls (`_raise_with_body`, commit `9d152fa`) -> revealed the real block: regional Fabric CU quota (`TotalCapacityUnits: 16, RegionalQuota: 16, RequestedSku: 32`) on sub `b55694da...`. Auth chain (WI -> AKV -> SP -> ARM read+PATCH attempt) now verified working. Researched the quota question online: reservations do not block scaling (billing-layer only, MS Learn scale-capacity); portal quota requests can auto-deny by sub type/region with support-ticket fallback (MS Learn fabric-quotas + Q&A precedent for the identical F8->F64 case); NEU/WEU capacity pressure documented (Aidan Finn 6/2026) but no official statement on lowered Fabric defaults - Niels's firsthand reports and the leita-sub (32) vs Carl Ras (16) delta remain unconfirmed as a deliberate turn-down (quota varies by sub type). Project status `planned` -> `active`; CONTEXT.md handed off; Carl-Ras unit repo committed (`e28cf8f`).
- **Decided:** Surface HTTP response bodies on all API errors in the notebook - `raise_for_status()` hid the AADSTS/ARM codes and cost two blind debug rounds (project Decisions Log).
- **Tasks:** `2026-07-07-carlras-fabric-scaleup` open -> **in-progress** (found at /log - the session skipped /switch-task at start, so 07-17 time is untagged; F&O code still unset in the fabric project identity block).
- **Next:** Carl Ras admin submits the Fabric CU quota increase (>=32, 64 preferred; support ticket if auto-denied - offer stands to draft the Danish mail). Then re-run `PL_ScaleProcess_SP`: verifies the scale PATCH and the still-open custom-role/capacity-admin question. Confirm sku_process/sku_idle + model workspace_id/dataset_id.

## 2026-07-28
### Matas — DataCompare: architecture v1 agreed, config store (Part 1) + canonical layer (Part 2) built (session ran 07-16 → 07-28)
- **Did:** Status intake (email 02 → **capacity approved**, in motion; email 03 sent, awaiting existing-link env/scope + GFO region). Strategy: **shortcuts over from-scratch** for MFO; GFO needs its own Link to Fabric regardless (separate Dataverse env). **Architecture v1** fixed via 15-decision one-card-at-a-time interview → `design/architecture.md` [D1–D15] (metadata-driven engine, entities = config rows, Fabric SQL DB config, global-party level, aggressive normalization, value-pair-bound acceptance, Pingala-only serving v1). **Part 1 (config store)** built via ultracode workflow (MS-Learn research → build → 3-lens adversarial verify → 2 fix rounds; 1 verify agent lost to the **monthly spend limit**, lens closed inline): 9-table `cfg` schema (idempotent, mirroring-aware), static seed (GFO master, Vendor + 29 fields, 3 match rules, VendorAccountNumber rule inactive [D7]), Excel→SQL generator + byte-deterministic map seed (NAV 59 + AX 62), deploy README. **"Powertable" identified** = PowerTable sheets in the Fabric IQ **plan (preview)** item — fits config editing; open: preview status, tenant prereqs, **no Entra B2B guest support** vs Pingala-maintained config [D1]. **Part 2 (canonical layer)** built inline: Fabric **Python** notebook (D365FO adapter, 11 Link-to-Fabric raw tables → `canonical_record` + long-format `canonical_value` at global-party grain, transformation maps + aggressive normalization) + README (link-scope table list, field mapping, first-data checklist); Link-to-Fabric behaviors verified vs MS Learn (IsDelete, derived-table exports, valid-time rows, numeric enums, 2k memo truncation). Both parts **UNVERIFIED against real data** (no lakehouse access yet). Matas unit repo commits `e356396` + `ea14ea2`.
- **Decided:** (project CONTEXT.md Decisions Log) shortcuts-over-scratch (07-23); architecture v1 [D1–D15] (07-25); **Python notebooks over PySpark** unless a compute need is identified (owner default → `pingala-fabric-platform` skill); **[D4] party aggregation rule** (07-28): one VendTable row per party — `party_priority` → company code → account number, ALL VendTable-level fields.
- **Tasks:** `matas-enhance-user-stories` is the session binder (in-progress); **`activity:` still blank** — F&O requires an Activity on every 212-01 time line; asked repeatedly, unanswered.
- **Next:** Build **Part 3 (match engine)**; chase email 03 + get the 11 canonical raw tables into both links' Manage-tables scope; ask Matas ([D7] account survival, [D9] active definition, MFO/GFO company codes, PowerTable direction). **INFRA:** session rooted below `C:\Dev` → no time/capture hooks fired — **DataCompare hours 07-16..28 need manual timesheet entry** (`hooks-subdir-session-gap`, second confirmed instance).
- **Memory:** `sentinel` agent type not loadable in this project-rooted session (workspace agent roster doesn't cascade, same family as the hooks gap) → daily/2026-07-28 vetted **by hand** per README recipe: 1 record, charset/markers/bounds clean, but it belongs to this morning's *other* workspace-rooted session (F16 / Warehouse_Curated — Carl-Ras by inference) → left `raw` for that session's own /log; nothing from THIS session was captured (hooks gap). Distilled from conversation: `eval-2026-07-28-datacompare-build` (evaluative); updated `hooks-subdir-session-gap` with the DataCompare instance + agent-roster extension.

### Workspace — hooks-subdir-session-gap FIXED: Dev-wide time/capture tracking (same session, post-/log)
- **Did:** Implemented the hook fix (owner mandate: "when and wherever Claude is opened inside Dev it must track time"). `track_time.py` + `capture_turn.py` registered in the **user-level `~/.claude/settings.json`** (byte-identical command strings to the workspace registration — Claude Code dedupes identical commands, docs-verified); both scripts rewrote attribution **anchored at `C:\Dev`** (`realpath`-canonicalized): any depth in a project → the project; a customer node → `customers/<client>` (new level, Proj ID `UNSET` at review); depth-3 folders without `CLAUDE.md` (grandfathered flat repos) → the customer; elsewhere under Dev → `Dev`; **outside `C:\Dev` → not tracked/captured at all**. capture scope gains `client:<name>`. Verified by **26 execute-and-assert subprocess tests** + a **3-lens adversarial workflow** (requirements refutation / claude-code-guide docs check / code review, 3/3 agents). The workflow killed one of my own additions as a **regression**: a Stop-dedup guard that would have truncated multi-Stop turns (yield-then-continue) — removed; every Stop writes, rollup merges. Docs aligned: `ops/time/README.md` §1–2 (levels, registration, known limits), `AGENTS.md`, `C:\Dev\CLAUDE.md` (Reminders + customer-walk billing sentence), `_templates/customer/` + **all 8 customer-node CLAUDE.md** ("no time attaches here" doctrine replaced), memory records `hooks-subdir-session-gap` (FIXED; agent-roster + snapshot cascade still open), `customer-project-two-tier`, `workspace-design`.
- **Decided:** Time attribution has **three levels** (project / customer-node / Dev), rolled up from any depth; sessions outside `C:\Dev` never tracked; command-string byte-identity across the two registrations is load-bearing. Customer-node doctrine softened: tasks stay project-only, node-level time surfaces as `UNSET` for review.
- **Next:** Fix applies to **newly opened** sessions (hooks snapshot at startup) — the current session's own hours still go in manually with the DataCompare back-hours (07-16..28). Open: `SessionStart` snapshot + agent roster still don't cascade (deliberate/unresolved respectively).

### Carl Ras / datahub — GTM medallion scaffold started: Raw built+verified, Enriched objects built, 14-mo load running (session continued from 07-16, this block 07-28)
- **Did:** Checked the live LZ hourly ingest — green (10 scheduled runs since the fix, delta-log commits track the schedule). Then began the **GTM medallion → DirectLake model → hourly-freshness report** build. **Raw:** authored `NB_Raw_GTM` (pure Python polars+deltalake; flattens LZ `dbo.events` 46-field GA4/Stape JSON → typed `events`, incremental on sequence_number, per-day batched), created `Lakehouse_Raw_GTM`, pushed to Fabric-ETL main (`a488d7d`, approved); flatten validated on 5,067 real bodies; workspace smoke-test green (1.03M rows). **Enriched:** created `Warehouse_Enriched_GTM`, built `viewtransform.Events` (business logic: EventCategory/IsWebVital/segment NULLIF) + `sp_CreateTableAsSelect` via **direct T-SQL** (pyodbc+ODBC18+`az` token — proved local→Fabric-warehouse DDL works). Launched **14-month Raw load** (full_reload, backfill_from=2025-06-01) as a server-side job — still running at wrap. Mapped shared-Curated conventions (reuse `dim.Date`, fact/dim SPs). **Folder bug:** `fab mkdir` created both data items at workspace root → moved LH→03-Raw/GTM, WH→02-Enriched/GTM via REST API (folders/moves are API-only); recorded prevention in the pingala-fabric skill.
- **Decided:** (with owner) DirectLake over import (refresh pain, cf. CapacityManager); **F16** for now; **13-month** fact window (F16 guardrail 300M rows/table, ~155M at 13mo); **shared `Warehouse_Curated`** for GTM fact/dims; **separate `Warehouse_Enriched_GTM`** per house standard; hybrid Python-Raw / SQL-warehouse-downstream. Standing instruction: **always prompt on folder placement when unsure.**
- **Tasks:** `2026-07-16-carlras-gtm-inbound-ingest` stays in-progress (activity **TagManager**); log + a **Resume facts** block (IDs, WH endpoint, pyodbc-DDL mechanism, Curated spec) added for cold restart.
- **Next:** confirm the 14-mo Raw load finished (`fab job run-status` id `31cd963b…`) → `EXEC transform.sp_CreateTableAsSelect 'Events'` → verify `enriched.Events` → build Curated `fact.GtmEvents`(13-mo)/`dim.GtmEvent` + DirectLake `Model_GTM` (Semantic-Model-DEV) + freshness report (Sales-DEV). **Commit to git next session:** Enriched WH objects + Raw LH folder relocation (workspace→git). Later: full-history Raw backfill; WI/SPN connection swap.
- **Memory:** sentinel not in this session's agent roster (known non-cascade) → daily/2026-07-28 hand-vetted; flagged 1 raw summary overstating `fact.GtmEvents`/`dim.GtmEvent` as "created" (only *designed*) — not carried. Distilled: `gtm-medallion-build` (project), `fabric-directlake-guardrails` + `fabric-warehouse-ddl-pyodbc` (reference), `eval-2026-07-28-carlras-fabric-folders` (evaluative: pingala-fabric-platform fired+helped but 2nd conventions-gap after lineage-viewer — folder placement — now patched into skill).

### Workspace + Carl Ras — time tracking reworked task-first; Carl-Ras/fabric merged into datahub; 34.25 h backfilled from transcripts
- **Did:** Reviewed the time system end-to-end (hook → heartbeats → rollup → timesheet) and confirmed the 07-28 dual-registration fix is live — sub-rooted sessions (DataCompare, Carl-Ras) now capture. Set `Carl-Ras/fabric` `fno_code: 230-02`. **Backfilled the pre-fix gap from Claude Code session transcripts:** +32.75 h billable / +1.50 h internal, 41 rows across 24 dates (5 new files), all 51 pre-existing rows byte-identical, no dates lost; every backfill heartbeat tagged `"source":"backfill"` (reversible). First attempt FAILED and was rolled back — turn-based reconstruction produced 42.50 h in a day, and re-rolling timesheets destroyed 8 dates / 28 hand-entered rows; corrected to point-events + additive-only. **Merged `Carl-Ras/fabric` into `Carl-Ras/datahub`** (both repos beneath it, both already 230-02 → billing-neutral; remotes proved it: both at `dev.azure.com/CarlRas/Datahub`); scale-up context folded into datahub CONTEXT.md as a Workstream, scale-up demoted project → task. **Built task-first attribution** (ADR-003): `track_time.py` inverted (active task decides project, cwd fallback, same-customer cap, session-scoped marker) + new `session_task.py` SessionStart hook (auto-sets when the project has exactly one open task, lists when several, offers task-or-project-level when none), dual-registered byte-identically. 8/8 attribution cases + 6/6 hook paths + end-to-end verified.
- **Decided:** **ADR-003** — attribution is task-first, capped to the same customer, `Dev`/`own/` never overridden; marker session-scoped so a stale tag cannot carry over. **A repo is not a project** — a project is ONE F&O Project ID, repos live inside it, distinct workstreams are tasks at **user-story / ADO work-item** granularity (sub-steps deliberately unmodelled — F&O has no dimension below Task). Folder-per-task **rejected** (forks task identity, only answers "am I tracked?" at launch; visibility belongs in the statusline).
- **Tasks:** `2026-07-07-carlras-fabric-scaleup` re-pointed to `customers/Carl-Ras/datahub` (stays in-progress).
- **Next:** Not built from the agreed plan — **statusline** showing project+active task, and a **/log catch** flagging untagged rows where the project has open tasks. **Needs your hand:** 9.00 h of stale Proj IDs in timesheets (Carl-Ras/fabric 4.50, Matas/DataCompare 3.50, own/DevFramework + own/AtomicCortex 1.00) — rows written before the codes existed, deliberately not auto-edited; 12.25 h sitting on the `customers/Carl-Ras` node at UNSET (≈half from the deleted `carl-ras\marketo` folder, inference: datahub/230-02); smaller UNSET node time on Vestforbraending/Melbye/Tystofte/Aeven/ElementLogic; 9 rows where reconstruction computes more than the sheet says. Backfill **undercounts** — 6 dates have heartbeats but no surviving transcript. Open: `own/CapacityManager` holds 4.75 h as internal though it was built inside Carl-Ras's repo.
- **Memory:** `sentinel` not dispatched (standing no-subagents instruction) → daily/2026-07-28 hand-vetted per README recipe: 9 records from this session, ASCII-clean, in bounds, **3 fidelity flags** (a fix attributed to this session that predated it; "34.25 h" relabelled as a Dev undercount when it was the total added; one garbled finding) → not distilled as-is. Distilled from conversation: `repo-vs-project-vs-task`, `time-backfill-from-transcripts`, `eval-2026-07-28-timetracking-rework`; updated `time-tracking-system` (task-first).

### Workspace — per-session task marker + unfinalized-days nudge (same session, post-/log)
- **Did:** Fixed a defect introduced by ADR-003 earlier the same day: `active-task` was a single shared record, so opening a second session cleared the first's tag — and concurrent sessions on different projects are normal here. Marker is now keyed by **session id**; each session reads only its own entry. Slash commands still cannot know the session id, so they write `unclaimed`, adopted by the next turn **only if the task passes the same-customer test** (stops a concurrent session on another customer stealing it); entries prune after 7 days; both older formats still read. Added the **unfinalized-days nudge** to `session_task.py` for every workspace session (`Dev`/`own` included): days with tracked time whose /log never ran. Verified 10/10 attribution cases, the two-session case that previously dropped a tag, and all nudge paths (excludes today + finalized days, silent outside `C:\Dev`). Commit `37ff425`, pushed.
- **Decided:** Session-scoping the marker was right; making it a *single* record was not — ADR-003 carries a dated same-day correction. The nudge stays a **reminder, not machinery**: leaving sessions open costs nothing in hours (15+5 discards idle gaps) and `rollup.py` finalizes missed days in bulk; only `CONTEXT.md` handoff and memory distillation genuinely decay, and neither is reconstructable after the fact. `/switch-task off` clearing all sessions' entries accepted as a known limitation (no session id to target; cost is project-level tracking).
- **Next:** Unbuilt and optional — a **scheduled nightly job** (rollup + OneDrive backup) so a backlog never forms; **statusline** showing project+active task; the `/log` untagged-row catch. Still needing your hand: **49.75 h of `UNSET` Proj IDs** (largest: `customers/Carl-Ras` node 12.25 h, `Tystofte` 8.00 h, `Melbye/data-agent-offer` 6.25 h) and `own/CapacityManager`'s 4.75 h possibly being billable Carl-Ras work. Today (2026-07-28) is still live — it finalizes at the next /log.

## 2026-07-30
### Matas — DataCompare: Dataverse/F&O environment-discovery tooling built and verified against two live tenants (session ran 07-29 → 07-30)
- **Did:** Built two PowerShell scripts under `DataCompare/src/env-discovery/` to answer, **without relying on environment names**, which Power Platform environments hold D365 F&O data and how each one exports it. **`List-DataverseEnvironments.ps1`** — tenant-wide, admin API only, no pac: one row per environment with `DataverseUrl`, `IsFinOpsLink`, `FinOpsHost`, `FinOpsLinkType` and the **specific** region (`properties.azureRegion` = `westeurope`, distinct from the BAP geo `Location` = `europe`, plus `crmGeo` / `cluster.uriSuffix` / `cluster.geoShortName`). **`Get-EnvironmentDataLinks.ps1`** — per-environment, **pac CLI only, zero PowerShell modules, no app registration** (customer constraint: "that will never happen"), runs on macOS, accepts `-EnvironmentUrl` / `-EnvironmentName` (display names with spaces) / `-EnvironmentCsv` + `-FinOpsOnly`; the two compose, the List- CSV feeds the Get- run. The classification problem — the portal label changed across versions ("Link to Synapse" → "Azure Synapse Link" → "data link") — was solved with four stored markers, each found empirically: F&O-backed = `properties.finOpsMetadata.url`; **real Azure Synapse** = a `datalakefolder` row with `isexternallake=1`; **Link to Fabric** = profile `extendedproperties` containing `"LinkedToFabric":true`; low-latency = the same profile also carrying `"EnabledForDlw":true` (**inference**, one correlated observation). Verified end to end against Vestforbrænding: kdev1 → FabricLinks 1, 867 Dataverse + 3 F&O tables, LowLatency 1; CRM Test → SynapseLakes 1, account `std365fobidatalaketest`, 72 F&O tables — **every selected count matches the portal** (871/3640 and 1004/3636 available respectively). Also produced the Matas input set (`environments.csv` — real admin-API output, 26 environments, 8 with `IsFinOpsLink=True`, all 8 `Linked`) and the recipient-admin setup + run instructions. Commits `684dd10`, `8269cc7`.
- **Decided:** Report only what is **selected**, never the "available" side — that number is metadata/catalog and is not queryable from Dataverse; the wizard's 3640 vs our `mserp_` count of 252 measure different populations, so the column was dropped rather than shipped wrong. Fabric **workspace name is out of scope**: it is not stored in Dataverse (`datalakeworkspace` holds Dataverse/ADLS workspaces — the user checked the returned names against Fabric and none matched), it needs the Fabric REST API. Keep both scripts independent rather than merging: the admin-API one must stay usable with no pac and no Dataverse role.
- **Tasks:** none moved. `2026-07-06-matas-enhance-user-stories` is still **open** and still the intended Matas binder — its `activity:` is **still blank** after being asked repeatedly across three sessions, and Matas 212-01 requires an Activity on every time line, so all Matas hours to date are unbillable as written.
- **Next:** The recipient admin runs `./Get-EnvironmentDataLinks.ps1 -AuthProfile env-discovery -EnvironmentCsv ./environments.csv -FinOpsOnly -CsvPath ./matas-links.csv` on his Mac (pwsh + .NET 8 + pac + `pac auth create --deviceCode`; needs a Dataverse security role per environment — Power Platform admin alone cannot read `synapselinkprofile`, and any environment lacking it returns `Status = ERROR` rather than a silent zero) → his `matas-links.csv` then answers the open MFO/GFO link-scope question from email 03. Then back to **DataCompare Part 3 (match engine)**. Uncommitted at wrap: the `SynapseAccounts` and dead-column fixes plus the CSV input files — `links.csv` is generated output and the Vestfor/Matas CSVs hold another customer's environment URLs and a storage-account name, so placement in the Matas repo is an open call (same reason `raw.txt` / `kdev1.txt` / `vftest.txt` were deleted earlier).
- **Time:** `rollup.py` finalized **07-28** (2.75 h billable / 5.25 h internal) and **07-29** (4.75 h billable: Matas/DataCompare 4.25, Carl-Ras/datahub 0.50). Today stays live. 07-28 carries four `UNSET` Proj IDs (Aeven/ServiceNowPOC 0.50, Carl-Ras node 0.50, own/CapacityManager 0.50) on top of the ~50 h `UNSET` backlog flagged at the last /log — still needs your hand.
- **Memory:** `sentinel` again not spawnable from a project-rooted session (4th consecutive /log; the one open leg of `hooks-subdir-session-gap`) → daily/2026-07-29 + 07-30 hand-vetted per the README recipe. Records are ASCII-clean and in bounds, but **four fidelity flags** — a summary stating the per-environment script "requires an app registration" (the *superseded* position; the delivered script needs none), a hallucinated "matching Adobe's example", "renamed the script to FnoVirtualTables" (a column, not the script), and "Installed .NET 8 SDK / installed pac tool" (instructions were given, nothing was installed) → **nothing distilled as-is**; both store records written from the conversation. Distilled: `dataverse-fno-link-discovery` (procedural — the four markers, the six wrong hypotheses, the pac/PS-5.1 traps), `eval-2026-07-30-env-discovery` (evaluative). Carl-Ras records in both streams belong to the concurrent session 0c39764b and were left `raw` for its own /log.
- **Evaluation:** **`pingala-fabric-platform` never fired across ~70 turns** whose subject was literally two of its stated triggers ("D365 F&O extraction", "Dataverse integration") — and no other workspace skill fired either. Two undistinguished causes: workspace `.claude/skills/` may not reach a project-rooted session (same family as the hooks gap; note the only *pointer* that resolved was `/dashboard`, which has a user-level copy) or the triggers missed operational phrasing. Worth a cheap test at a project root, because if it is the cascade then description tuning is wasted effort. Own-output failures the user caught, both `feedback-fact-only-language` of the same kind — **a number without its provenance**: I quoted a "108 Dataverse tables" figure from a superseded run of superseded code, having silently dropped the column that produced it; and reported table counts as authoritative before scoping them to the profile. Rule earned: when a column's definition changes, declare the old figure void — do not carry it forward and do not remove a column silently.

## 2026-07-31
### Workspace — env-discovery tooling promoted to `own/EnvDiscovery` + routed into the access skill
- **Did:** Moved the two Dataverse/F&O environment-discovery scripts out of `customers/Matas/DataCompare/src/env-discovery/` into a new project **`own/EnvDiscovery`** — scripts, README and both real-tenant CSVs, byte-verified against the Matas repo's `HEAD` blobs before the deletion was committed. Scaffolded `CLAUDE.md` (function / project / tooling / en, `INTERNAL-RND`, `lineage.from: customers/Matas/DataCompare`) and `CONTEXT.md` (state, open threads, the four 07-30 decisions + today's two), added the VS Code task, and pointed Matas `DataCompare/CONTEXT.md` forward under Related contexts. `own` turned out to already be a local unit repo with history — **I first reported it was not, from a check that only looked at its subfolders; corrected in-session.** `heal-repos.ps1 -Only C:\Dev\own` confirmed the wiring (`CapacityManager/capacity-control` + `Data-and-AI.wiki` ignored + guarded). **Routed the tooling into `fabric-project-access` as a new section 0 — Environment Discovery**: when to run the two scripts, the marker table carried inline (not just a path), the tenant-admin/no-tenant-admin split that lets two different people run the two steps, what discovery feeds into the access request (region from `azureRegion` **not** `Location`; the per-environment Dataverse security role; `(no Dataverse)` rows), and how to read a failed check. `pingala-project-playbook` gained Phase 1.0 + a checklist line; frontmatter gained the discovery triggers.
- **Decided:** The tooling is **not Matas-specific** — tenant discovery applies at the start of any engagement, so it becomes an internal project rather than staying in the customer repo. Both CSVs moved as **test data** (owner's call; they are the only verified input/output pair the scripts have) — the `own` repo is local with no remote. `fabric-project-access` is the right home over `pingala-fabric-platform`: pick the skill that owns the **question** (what does this tenant already have = Phase 1), not the one that owns the technology. Promotion convention captured as `tooling-promotion-customer-to-own`.
- **Fabric fact established (user-verified):** **Two different Dataverse environments CAN link to the same Fabric workspace**, each getting its own lakehouse, SQL endpoint and semantic model — verified in a live tenant by Niels, 2026-07-31. MS Learn documents **nothing either way** on that direction (checked today across the Link to Fabric page, the "unlock deep insights" page and the FAQ); what it does document is the **opposite** direction — "Today, a Dataverse environment links to a single Fabric workspace", multiple links per environment stated as planned. The two claims are kept apart in every place they were written so they cannot be conflated later. Also recorded: **two portal guides now exist** for creating a Fabric link (the older path under Azure Synapse Link, and the newer **Link data** page) producing the same link, differing in styling; `Tables > Analyze` now creates Fabric links only. Written into `fabric-project-access` section 0, `ops/memory/store/dataverse-fno-link-discovery.md`, and the EnvDiscovery README (whose "per Dataverse environment" bullet was the line most likely to be misread as one-workspace-one-environment).
- **Tasks:** none created or moved.
- **Next:** The Matas recipient-admin macOS run is still the open use (`Get-EnvironmentDataLinks.ps1` has never been run on macOS). The 07-30 **cheap test is still not run** — whether a workspace skill fires unprompted in a *project-rooted* session. The description edits made today are a live experiment at workspace level: check next session whether `fabric-project-access` fires on one of the new phrasings without being asked.
- **Memory:** `sentinel` not dispatched (standing no-subagents instruction, 5th consecutive /log) → `daily/2026-07-31.md` hand-vetted per the README recipe. Charset clean, no injection markers, all records in bounds, but **two fidelity flags** — the summarizer turned a **labelled inference into a bare fact** ("the system allows for multiple lakehouses in one workspace", where the turn explicitly refused to assert it), and one record says the `own/` repo was "left untouched" in a turn that committed to it. Neither distilled as-is; store records written from the conversation. Distilled: `tooling-promotion-customer-to-own` (procedural), `eval-2026-07-31-skills-available-not-firing` (evaluative); updated `dataverse-fno-link-discovery` (portal/topology block + new path). Records scoped to `customers/Matas/DataCompare` and `customers/Carl-Ras/datahub` belong to concurrent sessions and were left `raw` for their own /log.
- **Evaluation:** **At workspace root the skills are loaded and still did not fire.** Direct evidence they were present: the available-skills listing named `fabric-project-access` and **re-emitted with the new text** when its description was edited mid-session. Yet nothing fired across a session whose subjects were Link to Fabric, Dataverse environments and Azure Synapse Link — all named in two skills' own trigger lists. That narrows 07-30's undistinguished pair to **trigger-miss at workspace level**; the project-rooted leg (cause 1, the cascade) remains untested. Own-process: the Link-to-Fabric question was answered by going straight to MS Learn without first checking whether a skill covered it.

## 2026-08-01
### Workspace — harness cascade closed (agents/skills/commands now reach every project), dashboard internal-hours triage, 18.50 h reassigned (session ran 07-31 → 08-01)
- **Did:** Fixed the **month tile** (it showed billable as the headline while the week tile showed the total — same row, two meanings; the "77.5 h internal" was actually the period total, internal was 31). Then closed the last leg of `hooks-subdir-session-gap`: `M` was unspawnable from a project session, and the cause ran deeper than the roster — `ops/bin/heal-repos.ps1` > `Link-Harness` junctioned `commands` + `skills` but **never `agents`**, and ran only against unit roots and *nested git repos*, so plain-folder projects (`Matas/DataCompare`, `own/EnvDiscovery`) had **no `.claude/` at all**. 10 of 26 roots were provisioned, none with agents. Added `agents` to `Link-Harness` + `Get-ProjectRoots` (any dir with a `CLAUDE.md`, both storage depths); **all 26 roots** now carry junctions + a hard-linked `settings.json`, verified on a throwaway project and idempotent on re-run. Wired into `/new-project` (new step 6) so new projects in `own/` and `customers/` are provisioned by design. Built the **internal-hours triage panel** on the dashboard: joins each `Dev`/`own` stretch to its session, the projects co-worked in it, and the turn text from `ops/memory/daily/` (`id: <ts>Z-<session8>` is the join), with a Project → Task → Activity chooser and a plan export. Fixed `active_sessions()`, which reported every tag in `active-task` as billing — 4 of 6 were from closed windows, one had never emitted a heartbeat; liveness now derives from the last heartbeat (`live`/`idle`/`stale`). Applied the owner's 22-move plan: **15 timesheets edited, 18.50 h internal → billable**, backup taken first.
- **Decided:** Provisioning keys on **projects** (a `CLAUDE.md`), not repos — the provisioner and the session-rooting rule had never agreed. Held by two guarantees, `/new-project` on creation and `/log` re-running the healer every wrap-up, so a hand-made project self-heals. The dashboard **stays derive-only** (Guardrail 7): triage choices persist in `localStorage`, never the substrate, and the panel says "not yet applied to any timesheet" so decided ≠ done. Reassignment hours are **recomputed** by relabelling heartbeats and re-running the 15+5 model — the panel's per-session figures are inflated by fragmentation (38.25 h vs 31.50 h) and must never be written. `own/ → customer` moves are project-to-project, which `README.md` sec.2 excludes; the owner overrode this deliberately, so they are **flagged as deviations, not blocked**.
- **Tasks:** none created or moved. No task in the store carries an `fno_task`; two carry an `activity` (`marketoimport`, `TagManager`). The Matas 212-01 activity gap is still open and still blocking (unchanged for four sessions).
- **Next:** **Unverified — restart a project-rooted session and confirm `m` is an available agent type**; agents may be scanned differently from skills/commands through a junction. Clear the saved plan in the browser (`localStorage` still says 22 saved, now false). The general drill-down question is still open — which of any-project-to-sessions, clickable chart day, customer→project→task nesting, or per-task hours. `UNSET` F&O codes remain (51.25 h before this session's changes).
- **Time:** `rollup.py` finalized **2026-07-31** (4.00 h billable / 2.75 h internal) — it had never been finalized. Then 15 July days were edited surgically: rows spliced, totals recomputed, an audit block appended to each, **no file regenerated** (TRAP 2). July now **67.75 h billable / 13.75 h internal**, period total **81.50 h unchanged** — the invariant that proves hours moved rather than appeared. Three days held less than the model computed and were capped at zero with a `!` line recording the shortfall (07-07 `own/CapacityManager` 4.00 vs 4.75 wanted; 07-14 and 07-15 `Dev` by 0.25 each). 07-21's 0.50 h had no destination — relabelled, it merged into ElementLogic's existing stretch and changed no total; it was a floor artifact, not real time. Backup mirrored to OneDrive before writing.
- **Memory:** `sentinel` not dispatched (standing no-subagents instruction, 6th consecutive `/log`) → `daily/2026-08-01.md` hand-vetted per the README recipe. Four records, charset clean, in bounds, **two fidelity flags** — one turn summarised as "adjusted the hours for specific rows" when it was a **dry run** (nothing written but the one finalized day), and one conflating cause ("copying the plan first ensures allocations persist", when copying was needed *because* the reload would wipe them). Neither distilled as-is. Distilled: `time-reassignment-method` (procedural), `dashboard-internal-hours-triage` (procedural), `eval-2026-08-01-harness-cascade` (evaluative); updated `hooks-subdir-session-gap` to closed.
- **Evaluation:** **The 07-30 "no skill fired" mystery was never a trigger problem in project sessions** — those roots had no `.claude/` at all, so nothing *could* fire. Two faults had looked like one: the project-rooted leg was a provisioning gap (now fixed), the workspace-level leg is a genuine trigger miss and still stands. Lesson: check a capability is **present** before tuning how it is **described**; the cheap test proposed on 07-30 would have found this in one turn. Own-output failures: the panel shipped behind a tile-footer link whose hover state was light-on-light and effectively invisible — the owner could not find it and read the alert rows as the panel; and the reassignment analysis built `{key(r): r for r in rows}`, collapsing two casing variants of `own/CapacityManager` so one was silently lost, producing a 3.50 h phantom. **Caught by the period-total invariant, not by reading the code** — the same TRAP 3 already written down, hit again in new code. Third consecutive `/log` where the local summarizer asserted completed actions that had not happened; that is a model/prompt problem now, not a per-day vetting problem.

### Carl Ras — Datahub: Marketo proven extractable, then found to be mostly a copy of AX09 (session ran 07-31 → 08-01)
- **Did:** Ran the **first real Marketo Bulk Extracts** against `569-TGK-793` — 13 completed jobs, ~2.5 MB of the shared 500 MB/day. Built `tools/marketo_extract.py` (full create→enqueue→poll→fetch cycle; leads, activities, any custom object via `--entity co:<apiName>`, either date filter, `--hours` for cheap sizing, `--dry-run`). **Proved Read-Only Lead / Activity / Custom Object** by completed jobs rather than describe calls. **Proved the `updatedAt` filter is available** — via custom objects, the strictest test, since they offer no `createdAt` at all (`purchase_c` 370 rows, `orderLine_c` 1,018 rows on a 2-day window, no 1035); v0.1's three fallbacks are now dead. **Sized activities:** 535 rows / 234 KB per business hour → ~5 MB/day, ~170 MB per 31-day job, so backfill to 2021 is weeks not months and activities are **not** the 500 MB constraint. **Measured churn** on two lead cohorts (last week, and the same week a year ago): ~12% of the base last-updated today, ~68% within a week, all clustered at 07:08–07:13 daily — a batch job that reaches year-old records with the same signature as fresh ones. Then the finding that changed the picture: **`account_c` and `accountLink_c` are empty** (0 rows across three windows including their 2021 creation month, controls returning rows in the same window), only 2 of 8 custom objects hold data, and **no ERP key is populated on the person** across all 995 rows extracted. Rewrote `design/MARKETO_EXTRACTION.md` to **v0.2** with §5.1 as an explicit correction. Also made `data/` gitignored workspace-wide via `heal-repos.ps1`'s managed block.
- **Decided:** **Daily incremental, or full — weekly degenerates to a near-full extract** at ~68% weekly churn. **The open question is no longer "can we extract" but "is it worth extracting":** account data exists only denormalised onto each person, 0 of 227 person fields are `crmManaged` (so nothing arrived by native sync, AX09 is the likely origin), and `1 Visit Webpage` + `3 Click Link` are 47% of activity volume duplicating the GTM feed. **Secrets policy corrected mid-session:** local-only unit repos MAY hold credentials — they are the private backup — so `.secrets/` is barred only at repos with a remote, via `Ensure-Excludes` → `.git/info/exclude`, plus `C:\Dev`'s own `.gitignore`. `data/` stays ignored everywhere. Convention written into `AGENTS.md`; `Client Id.txt`'s staged un-tracking was reverted so nothing leaves the local repo.
- **Tasks:** `2026-07-07-carlras-impact-marketo-mail` **open → in-progress**, `activity: marketoimport` set (was blank). Its content is now materially better specified: the Impact mail has sharper questions because the data answered several of the originals.
- **Next:** **Establish the join to AX09 before anything else** — match the 995 extracted emails against AX09 contact/debitor emails and read the match rate; email is the only field present on 100% of Marketo persons. Then the redundancy comparison (what AX09 already holds that Marketo merely re-exports). Both are doable from our side with no Impact input and no further extraction. **Prerequisite: verify the authenticated tenant against the customer node's `tenant_id` (Guardrail 11) before touching AX09.** Then send the Impact mail. Untouched this session: the GTM Raw layer, and the Fabric scale-up still blocked on customer-side CU quota.
- **Memory:** `sentinel` not dispatched (standing no-subagents instruction, 7th consecutive `/log`) → `daily/2026-07-31.md` hand-vetted per the README recipe. 12 records for this session, charset clean and in bounds, but **four fidelity flags** — one **inverts** the explanation it summarises (`createdAt` described as the incremental mechanism, when the point was that it suffices only for append-only activities); one records the `.secrets/`-everywhere position **corrected later the same session**; one contains a sentence that was never a claim; one places the account denormalisation **in `purchase_c`/`orderLine_c`** when it is on the person record. Nothing distilled as-is; store records written from the conversation. Distilled: `carlras-marketo-source-findings` (project), `eval-2026-08-01-marketo-extraction` (evaluative); updated `feedback-response-style` and `gitignore-data-folders` in-session. **Capture gap:** no records after 13:03Z — roughly the last third of the session is absent from `daily/`, cause unknown.
- **Evaluation:** **Skills were present and still silent.** Project-rooted session after the 08-01 cascade fix, roster demonstrably loaded, and the subject was literally `timestamp-timezone-pipelines`' stated triggers — "watermarks, date filters sent to external APIs" — plus `medallion-migration-validation`'s "watermark and tracking table patterns". Neither fired. That removes the last ambiguity from 07-30/07-31: **trigger miss, now confirmed at both scopes**, not absence. Own-output failures, three of one kind — doing or claiming more than warranted: **generalised a rule further than asked** (told to ignore `data/`, also added `.secrets/` and pushed both to all nine units, which would have left credentials unbacked in the local repos that exist to back them up); **stated "this repo has no `.gitignore` at all"** from a grep that had only searched it for the word "secret"; and **closed a turn with an unstated-stakes menu** ("which do you want?") that the user could not parse. Rule earned: generalise only the rule you were asked for — the adjacent one that obviously belongs is a separate proposal. Plus a register failure: asked to explain something *plainly*, answered with bold headers, a table and `file:line` citations (recorded in `feedback-response-style`).

### customers/Matas/DataCompare — link scope verified in a live tenant, repo becomes the home, tenant-scoped credentials made a guardrail (session ran 07-31 → 08-01)
- **Did:** Answered the provenance question on the vendor link scope: **the 29 fields came from Matas's Excel, all 11 tables came from our research** — the sheet is a data-entity export and names no tables. Added `VendBankAccount` (cross-system match key for the later AX/BC rounds) and `DATAAREA`, and wrote the 13-table request as `emails/04-link-table-scope.md` with four questions for the link owner (the `MatasTransformationVendorStagingTable`, a possible custom legacy-vendor-id column, the F&O application version, `GlobalOptionsetMetadata`). Researched **outside MS Learn** for the first time on this project — entity resolution rather than migration validation is the field that owns our hard part. Wrote `src/canonical/probe_source_lakehouses.py`, a read-only first-data probe. Then the data landed: verified via `fab` that **all 13 tables arrived in both lakehouses** and that **every required column exists in all 11 tables the canonical layer reads** — including `DirPartyTable.primaryaddresslocation/primarycontactphone/primarycontactemail`, the single most fragile assumption in Part 2, which held. Cloned the Azure DevOps repo to `fabric/`, committed the README, then authored the probe **as a serialised Fabric notebook in the repo** (`.platform` v2 + `notebook-content.py`) so it arrives via Update from Git rather than paste.
- **Decided:** **`fabric/` is the home for all solution code; `src/` is design record only** — recorded in `CONTEXT.md`, a new `src/README.md`, and banners on both sub-READMEs. **Skip the Raw/Bronze layer** (the LZ shortcuts are already Delta; DataCompare's history lives in the Part 4 discrepancy facts, not in a source replica) and **keep Lakehouse+Python over Atomic's Warehouse+T-SQL** — a conscious break from the house artifact standard, flagged as the owner's call. Atomic otherwise maps almost one-to-one onto the five architecture parts. **Suffix-stripping is out** as a name-normalization default: `Matas A/S` (DK) vs `Matas AB` (SE) are different legal entities, so across DK/SE/NO/FI it manufactures false matches — the suffix becomes a country signal instead, and match-normalization must be separable from compare-normalization.
- **Guardrail added (AGENTS.md 11 — Tenant-scoped credentials):** `fab ls` in a Matas session returned the **Carl Ras** estate, because the CLI was still authenticated to whatever session ran last. Verify the tenant against the customer node before any `fab`/`az`/`pac` call; on mismatch stop and hand over the interactive login. `tenant_id:` + `account:` added to the customer-node template and to **all eight customer nodes** (Matas and Carl Ras filled from verified values, six TBD). Hook enforcement deliberately not built — three written layers first, mechanise only if it slips again.
- **Tasks:** none created or moved. `2026-07-06-matas-enhance-user-stories` still open and still the billing binder; **its `activity:` is still blank**, and 07-31's 2.75 h on 212-01 landed with `Activity = -`. Fifth session unchanged.
- **Next:** **Run the probe** (Update from Git in `GFO_DataCompare_ETL_Dev`, then run `NB_Probe_SourceLakehouses`). Its output settles three things that currently block Part 3: the legal-entity codes that seed `cfg.source_company`, whether `VatNum` can carry the only active deterministic match rule (fill rate + duplicate fan-out), and whether `AccountNum` survives MFO→GFO, which would reactivate [D7] and collapse most of the matching workstream. Also open: the repo **folder layout** before any engine artifact is created, and whether a Fabric SQL Database serialises to git cleanly.
- **Time:** `rollup.py` — no new days (the concurrent session had already finalized 07-31: **Matas/DataCompare 2.75 h billable**, after 0.75 h was reassigned to it from `Dev`). 08-01 stays live. Backup mirrored, nothing new to copy. The 212-01 Activity gap is the one item that needs the owner.
- **Memory:** `sentinel` not dispatched (standing no-subagents instruction, 7th consecutive `/log`) → this session's 18 records in `daily/2026-07-31.md` hand-vetted. Charset clean, in bounds, **four fidelity flags** — a user-run command reported as mine, "provided a command to bypass" GCM when no bypass was offered, an explicitly unconfirmed MFO/GFO mapping reported as "determined", and a git push summarised as "Pushed a commit to GFO_DataCompare_ETL_Dev" (conflating repo with workspace, the exact distinction that turn was about). Nothing distilled as-is. Distilled: `fabric-git-author-item`, `fno-vendor-gab-link-scope`, `nordic-vendor-entity-resolution` (procedural), `eval-2026-08-01-matas-datacompare` (evaluative).
- **Evaluation:** Skills fired **only when hand-invoked** (`pingala-fabric-platform`, `email-outlook-ready`). `fabric-project-access` was surfaced by the harness exactly on topic and I answered from context instead — a near-miss, not a win. Own-process failure, twice in one shape: **asserted a setup state instead of querying it with the tool already in hand** — planned "connect ETL_Dev to git" when `fab api workspaces/<id>/git/connection` showed it already connected, and declared `m` unspawnable from the agent list rather than treating it as provisioning. Rule earned: before telling the owner a setup step is needed, query the setup. Third, different shape: after `fabric/` was declared the home, I still proposed pasting the notebook into the UI — the owner had to ask for the git route their own decision implied. A decision recorded is not a decision applied.

## 2026-08-03
### Workspace — F&O time-entry page: per-company timesheets, 5 h consolidation, customer/company filters (session ran 08-01 → 08-03)
- **Did:** Continued 972e353e. Fixed the internal-hours panel's blind spot — it read **heartbeats**, which are immutable by design, so the 22 stretches already reassigned on 08-01 came back as candidates and `localStorage` restored the selections on top; a second Apply would have double-counted. It now reconciles against the timesheet: a stretch whose project has no internal hours left that day is marked applied and drops out (July: **18 hidden, 25 still open**), and stale saved choices self-prune. Built the **F&O time-entry page** — a route (`#timesheet/month|week1|week2`) with header links, removed from the dashboard proper: one block per internal company read live from `ops/TidsregInfo.xlsx` (stdlib zipfile+ElementTree, no exported copy), each row Date · Customer · Project · Proj ID · Activity · Task · Hours, TSV **Copy rows** per company. Month view carries its own current/previous switch. Added a **Totals per company** card (line counts follow the consolidate toggle, so it says how much typing each company costs), **company and customer filter chips** in a sticky bar, and a **Consolidated** toggle. Raised entry consolidation to **5 h** (`ENTRY_MERGE_THRESHOLD`) via a new optional `threshold` arg on `rollup.consolidate_week`. Fixed: header links brown→white, the server 404ing on any query string, and the wheel not scrolling the page.
- **Decided:** F&O takes **one timesheet per internal company**, so company is the outermost grouping — PING / PNO01 / Power. **Internal time is tracked but never entered**: its own block, excluded from "To enter in F&O" (68.25 h) but included in the period total (81.50 h) so the month reconciles. The entry page consolidates harder than `/time` (5 h vs 2 h) because it exists to be typed into F&O, not to describe how the days ran — `/time` is unchanged, verified. The **Totals card ignores the filters** on purpose: it is the reconciliation figure, not a view of the selection. Proj ID precedence: timesheet wins, sheet fills a gap and is marked `(sheet)`, disagreement is flagged `conflict` and never silently resolved.
- **Tasks:** none created or moved. Three items added to `ops/TODO.md` (sheet typo, two `?` Projektnr, summarizer revision).
- **Next:** **Unverified — restart a project-rooted session and confirm `m` is an available agent type** (still outstanding from 08-01). Enter July in F&O from the new page. The general drill-down question is still open (any-project→sessions, clickable chart day, customer→project→task nesting, per-task hours).
- **Time:** `rollup.py` finalized **08-01** (2.25 h) and **08-02** (0.50 h), both internal. August to date **3.75 h** (1.00 billable / 2.75 internal). July unchanged at **67.75 h billable / 13.75 h internal**, period total 81.50 h.
- **Memory:** `sentinel` not dispatched (standing no-subagents instruction, 7th consecutive `/log`) → `daily/2026-08-01..02` hand-vetted. **Two fidelity flags**: a turn summarised as "**Entered** internal time for July 2026" that entered nothing (4th consecutive `/log` with a fabricated completed action), and the copy-plan/persistence conflation carried from the last entry. Also confirmed the capture hook recorded the **`/log` command's expanded help text as the User body** on three turns — the defect already logged for `/handoff`, so it is not command-specific. Distilled: `fno-time-entry`, `eval-2026-08-03-fno-entry-page`.
- **Evaluation:** **Every defect the user reported was affordance or visibility; every one I caught myself was logic.** Theirs: the panel behind an invisible tile link; "July only shows PING" (all four blocks rendered, but PING's 49-row table was ~1400px in an 822px viewport); company pills built as jump links when filters were wanted; brown-on-teal header links. Mine, both caught by asserting an **invariant** rather than reading code: the 3.50 h phantom (period total must not change when time only moves sides) and a 15 h day created by the 5 h threshold (no day may exceed `DAY_CAP`). Logic gets verified because there are invariants to test against; presentation ships broken because there are none — and DOM assertions confirm structure while saying nothing about whether a human can see or use the thing. Browser screenshots were flaky for most of the session, which is exactly what would have caught all four.

### customers/Matas/DataCompare — Part 3 match engine built and pushed; probe blocked on a tenant permission
- **Did:** Opened on the probe: `NB_Probe_SourceLakehouses` was already in the workspace, but the Fabric portal's account picker in the connected Chrome offered only `nchristensen@pingala.eu` and `svc_CRMSandbox@vestfor.dk`, not the Matas account — and after the extension was installed in the right profile, the Matas user turned out **not to be allowed to create Fabric items** (admin asked; probe deferred). Built **Part 3, the match engine**, instead: `fabric/NB_Match_Engine.Notebook/` (`.platform` v2 + `notebook-content.py`, fresh `logicalId`), entity-agnostic, driven by `cfg.match_rule` + `cfg.pinned_match`, writing `match_xref` (accumulating) and `match_candidate` (review artifact). Verified it without a lakehouse: a scratchpad harness exec'd the notebook's pure-function cells against synthetic frames — **30 checks, all pass**; all 7 cells compile. One check failed first and was the *test's* error, not the code's (a cross-border pair scores 0.625 because the country mismatch enters the mean) — confirmed against the actual `token_sort_ratio` values before touching anything. Hand-invoked `microsoft-docs` to settle the hand-authored `.platform`: write a fresh GUID, duplicates fail Update-from-Git, and the per-item-type pages contradict `source-code-format` on where `version` sits — copy what Fabric emits. Pushed `692596b..05f0313` to the Matas ADO repo **after asking**; `CONTEXT.md` updated (Part 3, the 08-01 link verification, the permission blocker, Next Actions reordered so the probe is #1).
- **Decided:** **Workspace split (owner):** our items live in `GFO_DataCompare_dev`; the Link-to-Fabric managed lakehouses stay untouched in `GFO_DataCompare`, read cross-workspace by `abfss://` path — no copy of source data. **Four Part 3 rules:** a deterministic key carried by >1 party on *either* side matches nothing (never resolve ambiguity by picking one); `match_xref` is **sticky** — a contradicting hit is reported, not applied, and only a pin repoints a party, because identity churning day to day makes the daily diff meaningless; fuzzy **never** auto-matches [D8]; scoring weights the rule's fields equally and gates on the leading field, since `cfg.match_rule` has no weights column — a weighted variant needs a schema change, decided against real data. Fuzzy bounded by blocking on `AddressCountryRegionId`.
- **Tasks:** none created or moved. `2026-07-06-matas-enhance-user-stories` still open and still the billing binder; **`activity:` still blank** — 08-01's 0.50 h on 212-01 again landed with `Activity = -`. Sixth session unchanged.
- **Next:** **Run the probe** the moment the item-creation permission lands — it settles [D7] (does `VendorAccountNumber` survive MFO→GFO), whether `TaxExemptNumber` can carry the only active deterministic rule, the legal entities that seed `cfg.source_company`, and the volume the fuzzy layer has to handle. Then tune Part 3 config from it (rule activation, threshold, whether per-field weights justify a schema column) before starting Part 4.
- **Time:** `rollup.py` — no new days; 08-01 (1.00 billable / 2.25 internal) and 08-02 (0.50 internal) were already finalized by the concurrent session. 08-03 stays live. Backup mirrored (robocopy exit 1 = files copied). The 212-01 Activity gap is still the one item that needs the owner.
- **Memory:** `sentinel` not dispatched (standing no-subagents instruction, 8th consecutive `/log`) → this session's 5 records in `daily/2026-08-03.md` hand-vetted; the other 8 records belong to three concurrent sessions. Charset clean, in bounds. **One fidelity flag** (`daily/2026-08-03.md:178`): the owner's actions — installing the extension, asking their admin — summarised with the assistant as implied subject; same family as the 08-01/08-03 flags. Distilled: `datacompare-match-engine`, `eval-2026-08-03-matas-part3`.
- **Evaluation:** **`claude-in-chrome` should have fired and didn't.** Its description says to invoke it before any `mcp__claude-in-chrome__*` call; I loaded the tools via `ToolSearch` and drove the browser without it. Concrete cost: the owner had to ask "how can you identify which [Chrome] to use?" — the `list_connected_browsers` → `select_browser` / `switch_browser` flow is exactly what the skill carries. **Loading a tool is not loading the doctrine for using it, and the tool list gives no hint the doctrine exists.** `microsoft-docs` fired on request and paid for itself. `pingala-fabric-platform` didn't fire on building a Fabric notebook — correctly, the project's own Part 2 notebook was the better template.

## 2026-08-04
### own/MetaAtomic — consolidated the metadata/lineage capability; MetaAtomic becomes the product (session ran 08-03 → 08-04)
- **Did:** Opened on "this project has not seen much action" and established the facts: MetaAtomic is three sessions from 2026-05-21, ~1,470 lines that **have never executed**, no `tests/`, no validation against any database, and its code arrived whole in `ef603d7 Initialize own repo` with no commit of its own. Status was contradictory — `active` in `CONTEXT.md`, `paused` in `CLAUDE.md`. Then inventoried the related estate (LineageDocumentation, Tystofte-Fabric, Data-Discovery, EnvDiscovery) and wrote `docs/capability-map.md`: 17 capabilities with owner/duplicate/unique verdicts, plus a **field-level reconciliation** of MetaAtomic's nine `meta.*` tables against `lineage_engine/model.py`. The duplicate surface turned out to be exactly one thing — the store. Wrote **ADR 0008** (lineage node/edge model wins over `meta.*`), then **ADR 0009** superseding its home decision after the owner reversed it; 0008 marked *partially superseded* rather than rewritten. Created `ops/tasks/open/2026-08-03-metaatomic-consolidation.md` (6 steps, high). Updated MetaAtomic `CONTEXT.md`, created `LineageDocumentation/INBOX.md` and added a decisions-log row there. **Nothing was moved or committed** — a concurrent session was building the Element Logic UI in the same repo throughout.
- **Decided:** **The lineage node/edge model is the canonical store**, over MetaAtomic's `meta.*` Delta schema — it runs at 19,996 nodes and is adversarially verified; MetaAtomic's has never executed (ADR 0008, still stands). **`own/MetaAtomic` is the product** (ADR 0009): the EL engine + UI and Tystofte's catalog extraction and profiling both move in, and customer projects become **deployments that hold no engine code** — so drift is impossible rather than discouraged, since there is nothing local to fork. **Carl Ras is the portability gate**, not a demo: the reusability claim isn't accepted until a second Atomic installation runs with no engine changes. **Sequencing (owner):** finish the Element Logic build in place → move → then test on a new customer; suggested observable trigger is LineageDocumentation Next Actions 1-3 closed, with its Next Action 4 (semantic-model metadata distillation) deferred until after the move because it is new engine code. The move's pass/fail is re-running Element Logic from the new home and reproducing 19,996 / 18,279 / 0.
- **Found:** MetaAtomic's three unbuilt pieces — `helpers.py`, `profile_timestamps.py`, `generate_handover.py` — **already exist working** in Tystofte-Fabric (`nb_B_handover_docs`, `nb_C_profile_timestamps`). Building them would have been the third implementation of the same thing; marked superseded. The genuine gap in the shared core is **data profiling** — `column_timestamps`/`table_activity` answer "is this table actually used", and static SQL parsing cannot produce them.
- **Tasks:** `2026-08-03-metaatomic-consolidation` **created → open** (`own/MetaAtomic`, high), blocked on the Element Logic build finishing.
- **Next:** Owner finishes the initial Element Logic setup. Then: settle the uncommitted engine work in `customers/ElementLogic` (`model.py`, `html_report.py`, `graph_builder.py`, `enrich.py`, `cli.py`, untracked `tmdl_semantic.py`), move `lineage_engine/` into MetaAtomic, verify the run reproduces, fold in the Tystofte source side, then Carl Ras. Still open: whether the move preserves git history across two separate local-only repos, how deployments import MetaAtomic (path/submodule/package), and where constraints and indexes live in the flat Node/Edge model. The out-of-Dev third-party SQL Server extraction remains un-inventoried by the owner's choice.

## 2026-08-04
### customers/ElementLogic/LineageDocumentation — semantic layer + business descriptions; local-model backend (session ran 08-03 → 08-04)
- **Did:** Extended the engine over the Power BI semantic model (`tmdl_semantic.py`): TMDL parser (tab-indent blocks, `///` descriptions, bare-keyword flags like `isHidden`, fenced expressions) building semantic tables, columns, measures and calculation items, with DAX-reference-extracted edges and relationships as a separate non-traversed `edge_kind`. Reworked the viewer for it — Semantic view, collapsed layer boxes with per-kind counts, tables/views split, a Power BI-style collapsible measure tree, hidden-and-unreferenced objects filtered out of the default view, two clearly separated description blocks. Built **`lineage_engine.describe`**: evidence assembled from the lineage (a column's chain with only non-trivial transformations named; a measure's full DAX plus its **transitive** measure closure and the chains those read), fingerprinted store at `descriptions/descriptions.json`, merge-back into `lineage.json`. Added a **local Ollama backend as the default**, with per-group checkpointing, `--group-size`/`--ollama-ctx`, `--ollama-url` + `LINEAGE_OLLAMA_URL` for a GPU box on the LAN, a preflight that proves reachability *and* model presence before a multi-hour run, and live tok/s. README documents the whole remote-GPU setup. 12-object pilot run end-to-end.
- **Decided:** **Local model is the default backend** (ADR-001 Local Model Offload) — decisive reason is not cost but that no customer metadata leaves the network, which **dissolved the credentials + egress blocker** rather than routing it through an approval conversation; `--backend anthropic` (Message Batches, `claude-opus-5`) stays as the fallback. Descriptions are **derived, never authored-in**: the TMDL `description` is passed as an explicitly unreliable hint because in this model it restates the DAX. Descriptions are a **scheduled feature, not a one-off** — hence the fingerprint store, the `reviewed` state that is never regenerated, and checkpointing. Run order is **engine → describe**. Measures are `grain: column` + `node_type: measure` so one traversal mechanism serves measure → measure → column. Generation runs **on our side** (Pingala executes, store committed to the repo) — no API key into the customer tenant.
- **Tasks:** none created or moved. Session time tracked at **project level** (6001-01) as the owner asked, not against a work item.
- **Next:** **Prompt tuning is the open work** — a 12B reads the lineage correctly (surfaced a source field four layers up, read `ALL('DimDates'[CalendarDate])` as ignoring date filters) but describes *mechanics* rather than business meaning, restates the object's name when evidence is thin despite an explicit rule, and is inconsistent between related measures. Blocked on the owner confirming `ollama ps` shows `100% GPU` on the 5060 Ti box and picking a 20–24B model; delete `descriptions/descriptions.json` first for a clean benchmark against the 1.25 tok/s CPU baseline. Then the full run (~1 h on GPU vs ~24 h on CPU). Still parked: READS grid rework, TMDL write-back of descriptions (round-trip ownership unresolved), the `reviewed` human-override state. **`CONTEXT.md` still describes the pre-semantic-layer state** — flagged repeatedly across the session and still unaddressed. Standing item unchanged: tell Element Logic to rotate the service-principal secret committed in `NB_Manage_Shortcut_LZ_To_Raw` lines 171-173.
- **Time:** `rollup.py` finalized **08-03**: billable 13.25 h (Carl-Ras/datahub 7.25, **ElementLogic/LineageDocumentation 4.75 @ 6001-01**, Matas/DataCompare 1.25), internal 1.00 h (Dev 0.50, own/MetaAtomic 0.50). No `UNSET` Proj IDs. The Matas **212-01 `Activity` is still blank** — seventh session, still the one item needing the owner. Backup mirrored.
- **Memory:** `sentinel` not dispatched (standing no-subagents instruction, 9th consecutive `/log`) → this session's records in `daily/2026-08-03.md` hand-vetted. **Two fidelity flags:** "ensured credentials are set for API calls" when the turn reported the exact opposite — no credentials exist, owner asked to provide them (**5th consecutive `/log` with a fabricated completed action**); and the LAN setup summarised as "on this PC, set the `OLLAMA_HOST` environment variable", conflating the *server* variable with the client's `LINEAGE_OLLAMA_URL` — following it would fail. One shape flag: the GPU-model turn summarised down to `--group-size 16` reducing prompt repeats, missing the entire model-tier analysis that was the answer. **Capture gap:** this session (`a77891ac`) has **no records after 20:57 UTC**, so every post-compaction turn - the whole local-model pivot, the GPU sizing and the LAN setup - is uncaptured; the store records below were written from the live conversation, not from the stream. `daily/2026-08-04.md` holds exactly one record and it belongs to the concurrent `1b309702` session - whose User body is again **the `/log` command's expanded help text**, the known capture defect now on a fourth date. Distilled: `lineage-business-descriptions`, `ollama-remote-gpu-lan`, `local-llm-hardware-throughput` (all workspace-scope, portable beyond Element Logic), `eval-2026-08-04-provider-before-skill`; `atomic-lineage-engine` updated (semantic layer landed, its parked "business-language metadata" phase is now `describe`).
- **Evaluation:** **Settle the provider before invoking a provider-specific skill.** `claude-api` was loaded because the task was LLM-shaped and the provider unstated, and produced a sound cloud backend — which the owner then redirected with "can we try to use our local model for this?". Everything needed to propose local first was already in hand: ADR-001 names *documentation generation* and *client-sensitive data* as the two offload drivers, and Ollama is already in production here (the memory capture hook uses it). Sharper version of the failure: I **did** raise the egress concern and list exactly what would leave the machine, but framed it as an approval question for the owner instead of as a reason to pick a different provider — **surfacing a risk is not the same as acting on it**. A vendor-named skill will never ask whether that vendor is the right choice; that question lives upstream of it. No skill fired automatically all session.

## 2026-08-06
### Dev — value-based billing model built and shipped provisional (session ran 08-05 → 08-06)
- **Did:** Opened on "actual time spent does not reflect the value delivered". Measured the substrate end to end: **13.82 h of billable keyboard time across 23 working days produced 58.00 h on the timesheet** — so the current model already bills at **4.2x** measured production, blindly, via idle gaps, the tail buffer and the 0.5 h floor. Found **two capture defects that inflate today's numbers independently of any value model**: a `Stop` hook that does not always fire (2026-08-03, a 1-minute turn recorded a **340-minute** heartbeat because the hook fired only at the next prompt 5.5 h later — `ops/time/README.md:49` documents many-beats-per-turn but not this), and **intra-turn dead time** (2026-07-23, a 131-minute turn containing **8.8 minutes** of activity: an 89.6-minute gap before a tool result, 33 minutes before the next reply). Built the model against 579 turns, 98% matched to transcripts, iterating through four wrong versions before the numbers held. Shipped **`ops/time/value.py`** (derive-only, ASCII-clean, imports `rollup.py` so F&O dimensions stay single-sourced), **ADR-004**, **`ops/time/README.md` §7** (tool-neutral spec + by-hand recipe, Guardrail 7), `.claude/commands/log.md` steps 5-6, `.gitignore` for `/ops/time/value/`. Derived 26 days; two `REVIEW` flags (08-03 18.50 h, 08-04 16.75 h across all customers, no single customer over 9 h).
- **Decided:** **Two numbers per F&O line: keyboard hours (measured) and weighted hours (derived)** — ADR-004, status **Accepted PROVISIONAL**, re-evaluate end of 2026-08. Turn boundaries come from the **transcript bounded by the heartbeat** (transcript ends a turn whose `Stop` never fired; heartbeat bounds a turn whose segmentation broke on a resumed session); intra-turn gaps capped at 5 min, the same rule already used between turns applied one level down — 3,052 raw heartbeat minutes become **1,245 active minutes**. Five tiers from tool evidence, T5 per *stretch*; **the `files >= 8` gate was dropped** — it fired twice in the month and was wrong both times (workspace config edits billed to Matas). **Deliverable classes weight the line count, not the hours** — knowledge (`CONTEXT.md`/`README.md`/`docs/`) at 2.0x on the owner's call that information is denser than code, `ops/memory/` 0.5x, `ops/tasks|time/` and `ops/memory/daily/` at 0 (the last on fact: the local summarizer writes them, not the engagement). **T5 stays T&M** — fixed price and a per-deliverable scope floor were both considered and rejected; a T5 event is evidence that a subsystem was built, not a deliverable (8 events mapped to **6 deliverables**, the lineage engine alone accounting for three). **Caps are per customer, not per day** (owner): 9 h/customer/day hard and spilling, 15 h across customers a soft review flag, 24 h an assertion — scoping it this way **halved the date-shifting**, 14.50 h moved to 7.75 h. Spill is `consolidate_week` run backwards plus two guardrails it does not need: never cross a month boundary (may be invoiced) and distance beats the worked-day preference (without it hours moved 13 days backwards).
- **Found:** The **fallback attribution was unbounded** — 37 turns (5.5%) had no covering heartbeat and borrowed the project from the nearest one in the same session at any distance. Bounded to 30 minutes and surfaced per row in the audit record; Matas dropped **32.75 → 28.00 h**, the difference being `.claude/hooks` and `AGENTS.md` edits sitting on a customer line. Also: **"transcripts start 07-07" was wrong** — that was file mtime, not content, which reaches back to 2026-05-26, so 26 days derived rather than 20 (the extra days are all internal, no conclusion changed).
- **Tasks:** none created or moved. Session time is **Dev / INTERNAL-RND** throughout — this is workspace tooling, correctly attributed.
- **Next:** **Run both models through August and re-evaluate at month end** — the four questions are in ADR-004 > Evaluation plan and `/log` step 5 now asks the fourth every session. The one that matters: **does GTM or Marketo complete?** Either gives the first genuine out-of-sample test of T5. Open and unresolved: high-value/low-line work reads too cheap (a 40-line DAX fix that unblocks a go-live scores T4, not T5), and **14% of billable weighted hours sit in stretches that wrote no file at all** — advisory work that carries no deliverable evidence, so no part of the tier model can validate it.
- **Time:** `rollup.py` finalized **08-04** (5.00 billable / 0.50 internal) and **08-05** (2.25 billable / 3.25 internal). No `UNSET` Proj IDs. The Matas **212-01 `Activity` is still blank — eighth session**, still the one item needing the owner. `value.py` for August to date: **6.10 h keyboard → 49.25 h weighted (8.07x)**, ElementLogic 25.75, Carl-Ras 15.25, Matas 8.25. Backup mirrored.
- **Memory:** `sentinel` not dispatched (standing no-subagents instruction, **10th consecutive** `/log`) → this session's 22 records in `daily/2026-08-05..06.md` hand-vetted. **Four fidelity flags, one serious:** a summary asserting the 40-hour anchor "**is considered evidence**" when the turn's entire conclusion was that it is *not* (the exact claim ADR-004 warns against); "60 hours, which is significantly higher than the estimated 80 hours" (inverted — the owner said quote 80, settle at 60); "a 5-minute prompt results in **0.25 hours**" (it is 0.50 — the floor was the point of that turn); and 19.75 h described as "based on the original estimate" when it is derived from nothing but the transcript. Plus **two fabricated completed actions** ("implemented a solution", "decided to implement deliverable bands" — bands were proposed and then rejected), the **6th consecutive** `/log` carrying that family. Distilled: `value-model-billing`, `time-capture-defects`, `eval-2026-08-06-fitted-is-not-validated`.
- **Evaluation:** **I reported a fitted parameter as if it were a prediction, and the owner caught it, not me.** I swept T5 x at 6/10/15/20/25/30 *after* being told the deliverable was worth ~40 h, then presented "the model produces exactly 40.00 h" as striking convergence — one equation, one unknown, always solvable. The owner's "seem very on point" was the correction. The out-of-sample check I then ran (GTM 13%, Marketo 19% of anchor) was inconclusive because both deliverables are unfinished, which is the real finding: **with one completed deliverable the model is exactly determined and untestable**, and no analysis fixes that. It is now written into ADR-004 as a named limitation rather than a result. Second, smaller: I proposed **three mechanisms the owner rejected in turn** — fixed-price deliverables, deliverable bands, a scope floor — each time reaching for machinery before establishing that the simpler thing was insufficient. No skill fired all session and none should have; this was substrate design.
